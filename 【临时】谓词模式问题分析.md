# 谓词模式问题分析

**分析日期**: 2026-08-03  
**问题**: 仅前N金额改为谓词模式，数量调整到20条，但不生效

---

## 问题描述

用户操作：
1. 将"仅前N金额"从"排行"切换为"谓词"
2. 数量选择20
3. 期望结果：显示金额最大的前20条（金额>0的前提下）
4. 实际结果：显示所有金额>0的数据（可能超过20条）

---

## 根因分析

### 当前 `applyToggleableFilter` 实现

```javascript
function applyToggleableFilter(data, mode, selectId, field) {
    const el = document.getElementById(selectId);
    if (!el) return data;
    const n = parseInt(el.value) || 0;
    
    if (mode === 'predicate') {
        // 【问题】谓词模式只检查 > 0，完全不考虑 n 的值
        return data.filter(it => (parseFloat(it[field]) || 0) > 0);
    }
    
    // ranking 模式才使用 n
    if (n <= 0) return data;
    const sorted = data.slice()
        .filter(it => (parseFloat(it[field]) || 0) > 0)
        .sort((a, b) => (parseFloat(b[field]) || 0) - (parseFloat(a[field]) || 0));
    const codes = {};
    for (let i = 0; i < Math.min(n, sorted.length); i++) codes[sorted[i].code] = true;
    return data.filter(it => codes[it.code]);
}
```

### 问题定位

**谓词模式的语义问题**:
- 当前实现：谓词 = 只检查条件（金额>0），不限数量
- 用户期望：谓词 = 在符合条件的里面取前N条

**但这里有一个概念混淆**:
- "仅前N金额"这个名称本身就暗示了"取前N条"
- 如果用户想要"谓词"语义，应该选择其他过滤器（如"仅金额>0"）
- 用户选择"仅前N金额"，无论模式是"排行"还是"谓词"，都应该体现"前N"的语义

---

## 修复方案

### 方案A：修改谓词模式，使其也考虑 n 值（推荐）

谓词模式应该：
1. 先过滤出金额>0的数据
2. 再按金额排序
3. 最后取前N条

```javascript
function applyToggleableFilter(data, mode, selectId, field) {
    const el = document.getElementById(selectId);
    if (!el) return data;
    const n = parseInt(el.value) || 0;
    
    if (mode === 'predicate') {
        // 【修复】谓词模式也考虑 n 值
        // 1. 先过滤出金额>0的数据
        const filtered = data.filter(it => (parseFloat(it[field]) || 0) > 0);
        
        // 2. 如果 n > 0，取前N条
        if (n > 0) {
            const sorted = filtered.sort((a, b) => (parseFloat(b[field]) || 0) - (parseFloat(a[field]) || 0));
            return sorted.slice(0, n);
        }
        
        // n <= 0 时返回所有符合条件的数据
        return filtered;
    }
    
    // ranking 模式保持不变
    if (n <= 0) return data;
    const sorted = data.slice()
        .filter(it => (parseFloat(it[field]) || 0) > 0)
        .sort((a, b) => (parseFloat(b[field]) || 0) - (parseFloat(a[field]) || 0));
    const codes = {};
    for (let i = 0; i < Math.min(n, sorted.length); i++) codes[sorted[i].code] = true;
    return data.filter(it => codes[it.code]);
}
```

**区别**:
- `predicate` 模式：先过滤再排序取前N（保持原始数据顺序的其他字段）
- `ranking` 模式：先排序取前N，再从原始数据中筛选（保持原始数据顺序）

### 方案B：修改过滤器名称和语义

将"仅前N金额"改为"金额过滤"，并明确区分：
- "金额过滤（谓词）" = 只保留金额>0的，不限数量
- "金额前N（排行）" = 取金额最大的前N条

但这需要修改UI和配置，影响较大。

### 方案C：移除谓词/排行切换，固定为排行

对于"仅前N金额"、"仅前N区间次数"等过滤器，强制使用排行模式，不允许切换。

```javascript
topn_amount:  { label: '仅前N金额', kind: 'ranking', fixed: true, ... },  // fixed: true
```

---

## 推荐的修复方案：方案A

理由：
1. 保持向后兼容（ranking模式行为不变）
2. 满足用户期望（谓词模式也体现"前N"语义）
3. 改动最小，只修改一个函数

---

## 验证方法

1. 设置"仅前N金额"为谓词模式，数量=20
2. 观察结果是否只有20条（或更少，如果金额>0的不足20条）
3. 检查这20条是否按金额从大到小排列
4. 切换到排行模式，对比结果是否一致

---

**等待用户审核修复方案**
