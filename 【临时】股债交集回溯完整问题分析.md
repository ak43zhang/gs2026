# 股债交集回溯完整问题分析

**分析日期**: 2026-08-04  
**问题**: 1) 无数据返回；2) 缺少开始/结束时间；3) 后端管道条件不完整

---

## 问题1：无数据返回（8月3日09:53:57，条件000703）

### 可能原因

1. **时间戳格式不匹配**
   - 前端传递: `09:53:57`
   - 数据库存储: 可能是 `09:53:00` 或 `09:53`

2. **过滤条件过于严格**
   - 股票过滤后无数据
   - 债券过滤后无数据
   - 交集为空

3. **字段缺失**
   - `bond_code` 字段不存在或为空
   - `amount` 字段为0

---

## 问题2：缺少开始/结束时间筛选

### 需求
- 当前：遍历全天所有时间戳
- 期望：可以设置开始时间和结束时间，只回溯指定区间

### 好处
1. 加快执行流程
2. 排查更方便
3. 功能更加完善

---

## 问题3：后端管道条件不完整

### 前端股票条件（6个）

| 条件 | 类型 | 后端支持 |
|------|------|----------|
| 行业联动 | predicate | ❌ 缺失 |
| 转债模式 | predicate | ✅ BondExistsFilter |
| 仅行业次数前N | ranking | ✅ TopNSectorsFilter |
| 仅行业涨幅前N | ranking | ✅ TopNSectorsPctFilter |
| 仅前N区间次数 | ranking | ✅ TopNWindowFilter |
| 仅前N次数 | ranking | ✅ TopNCountFilter |

### 前端债券条件（7个）

| 条件 | 类型 | 后端支持 |
|------|------|----------|
| 行业联动 | predicate | ❌ 缺失 |
| 仅行业次数前N | ranking | ✅ TopNSectorsFilter |
| 仅行业涨幅前N | ranking | ✅ TopNSectorsPctFilter |
| 隐藏绿名单 | predicate | ✅ GreenListFilter |
| 仅前N金额 | ranking | ✅ TopNAmountFilter |
| 仅前N区间次数 | ranking | ✅ TopNWindowFilter |
| 仅前N次数 | ranking | ✅ TopNCountFilter |

### 缺失的过滤器

1. **行业联动过滤器（股票/债券共用）**
   - 类型: predicate
   - 逻辑: 筛选指定行业的股票/债券

2. **谓词/排行模式切换支持**
   - 当前后端只支持 ranking 模式
   - 需要支持 predicate 模式（只检查条件，不限数量）

---

## 修复方案

### 修复1：添加开始/结束时间参数

**修改文件**: `backtrace.py`

```python
# API 添加 start_time 和 end_time 参数
date = data.get('date')
start_time = data.get('start_time')  # 新增
end_time = data.get('end_time')      # 新增

# 过滤时间戳
timestamps = get_timestamps(date)
if start_time:
    timestamps = [t for t in timestamps if t >= start_time]
if end_time:
    timestamps = [t for t in timestamps if t <= end_time]
```

### 修复2：添加行业联动过滤器

**修改文件**: `filters.py`

已存在 `IndustryFilter`，但需要确认 `pipeline.py` 中是否正确使用。

### 修复3：添加谓词/排行模式支持

**修改文件**: `config.py` 和 `pipeline.py`

需要为每个过滤器添加 `mode` 参数（predicate/ranking）。

### 修复4：调试日志

添加详细日志，帮助排查无数据问题：

```python
logger.info(f"时间点 {time_str}: 原始股票 {len(stock_data)}, 过滤后 {len(filtered_stocks)}")
logger.info(f"时间点 {time_str}: 原始债券 {len(bond_data)}, 过滤后 {len(filtered_bonds)}")
logger.info(f"时间点 {time_str}: 交集 {len(intersection)}")
```

---

## 验证步骤

1. 设置开始时间 09:53:00，结束时间 09:54:00
2. 选择8月3日
3. 设置宽松条件（如只选"转债模式"）
4. 执行回溯
5. 查看日志，确认：
   - 时间戳是否正确获取
   - 原始数据量
   - 过滤后数据量
   - 交集数据量

---

**等待用户确认修复方案**
