# 股债交集回溯 - 10分钟窗口汇总方案（最终版）

**版本**: v2.0  
**日期**: 2026-08-04  
**状态**: 待审核  

---

## 一、需求确认

| 需求 | 方案 |
|------|------|
| 窗口大小 | **固定10分钟** |
| 排序方式 | **窗口顺序 + 命中开始时间** |
| 展示方式 | **汇总为主 + 明细可展开** |
| 新增字段 | **时间差**（开始→最高）、**涨幅差**（最高-开始） |

---

## 二、数据结构

### 2.1 汇总数据（后端聚合后返回）

```json
{
  "window": "14:20-14:30",
  "window_start": "14:20:00",
  "window_end": "14:29:57",
  "pairs": [
    {
      "stock_code": "000703",
      "stock_name": "恒逸石化",
      "bond_code": "127067", 
      "bond_name": "恒逸转2",
      "industry_name": "化学纤维",
      
      "first_appear_time": "14:23:15",
      "first_change_pct": 0.37,
      
      "max_change_pct": 1.25,
      "max_change_time": "14:27:42",
      
      "time_to_max_seconds": 267,
      "time_to_max_display": "4分27秒",
      
      "gain_to_max": 0.88,
      
      "appear_count": 5,
      "last_change_pct": 1.10,
      "min_change_pct": 0.30,
      
      "details": [
        {"time": "14:23:15", "change_pct": 0.37, "is_first": true},
        {"time": "14:24:00", "change_pct": 0.52},
        {"time": "14:25:00", "change_pct": 0.89},
        {"time": "14:27:42", "change_pct": 1.25, "is_max": true},
        {"time": "14:29:00", "change_pct": 1.10}
      ]
    }
  ]
}
```

### 2.2 汇总字段说明

| 字段 | 说明 | 计算 |
|------|------|------|
| `window` | 窗口标识 | `14:20-14:30` |
| `first_appear_time` | **首次命中时间** | 窗口内第一次出现的时间 |
| `first_change_pct` | **开始涨幅** | 首次出现时的涨幅 |
| `max_change_time` | **最高涨幅时间** | 窗口内涨幅最大的时间 |
| `max_change_pct` | **最高涨幅** | 窗口内最大涨幅值 |
| `time_to_max_seconds` | **时间差(秒)** | max_time - first_time |
| `time_to_max_display` | **时间差(显示)** | `4分27秒` 人性化格式 |
| `gain_to_max` | **涨幅差** | max_pct - first_pct |
| `appear_count` | 出现次数 | 窗口内tick数 |
| `last_change_pct` | 结束涨幅 | 窗口最后一次涨幅 |
| `min_change_pct` | 最低涨幅 | 窗口内最小涨幅 |
| `details` | 明细数据 | 用于展开查看 |

---

## 三、展示设计（分析友好）

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  窗口筛选器 [全部] [09:30-09:40] [09:40-09:50] ... (横向滚动)   │
├─────────────────────────────────────────────────────────────┤
│  汇总表格（主视图）                                            │
│  ┌────┬──────┬──────┬─────┬─────┬────────┬────────┬──────┐ │
│  │窗口│股票  │转债  │行业 │次数 │开始涨幅│时间差  │涨幅差│ │
│  ├────┼──────┼──────┼─────┼─────┼────────┼────────┼──────┤ │
│  │... │...   │...   │...  │...  │...     │...     │...   │ │
│  └────┴──────┴──────┴─────┴─────┴────────┴────────┴──────┘ │
├─────────────────────────────────────────────────────────────┤
│  点击行展开 ▼ 明细时间轴                                       │
│  14:23:15 ───●─── 14:25:00 ─────●───── 14:27:42               │
│  0.37%      0.89%              1.25%(最高)                    │
│  [首次]                        [4分27秒后到达]                │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 汇总表格列设计

| 列 | 宽度 | 说明 |
|----|------|------|
| 窗口 | 100px | `14:20-14:30` |
| 股票 | 140px | `000703 恒逸石化` |
| 转债 | 140px | `127067 恒逸转2` |
| 行业 | 100px | `化学纤维` |
| 次数 | 60px | 居中，数字 |
| 开始涨幅 | 80px | 带颜色（红涨绿跌） |
| **时间差** | 90px | **重点列**，`4分27秒` |
| **涨幅差** | 80px | **重点列**，`+0.88%`，带颜色 |
| 最高涨幅 | 80px | 带颜色 |
| 操作 | 60px | `展开▼` 按钮 |

### 3.3 展开明细设计（时间轴样式）

```
窗口: 14:20-14:30 | 股票: 000703 恒逸石化 | 转债: 127067 恒逸转2

时间轴:
14:20:00 ──○── 14:23:15 ──●── 14:25:00 ──●── 14:27:42 ──○── 14:29:57
           │    [首次]          │              [最高]            │
           │    0.37%           0.89%          1.25%            1.10%
           │    ▲开始          │              ▲4分27秒后        (结束)
           │                   │              涨幅+0.88%
           └───────────────────┴──────────────┘
           时间差: 4分27秒 | 涨幅差: +0.88%

明细数据表:
| 时间     | 涨幅   | 窗口数 | 排名 | 主力净额 |
|----------|--------|--------|------|----------|
| 14:23:15 | 0.37%  | 3      | 5    | 1250万   |
| 14:24:00 | 0.52%  | 3      | 4    | 2100万   |
| ...      | ...    | ...    | ...  | ...      |
```

### 3.4 关键视觉设计

1. **时间差列**：
   - `< 2分钟`：绿色 🟢（快速拉升）
   - `2-5分钟`：黄色 🟡（正常）
   - `> 5分钟`：红色 🔴（缓慢）

2. **涨幅差列**：
   - `> 2%`：深红（强势）
   - `1-2%`：浅红
   - `< 1%`：灰色

3. **行高亮**：
   - 鼠标悬停整行高亮
   - 点击行展开明细，其他行收起

---

## 四、排序规则

**主排序**：窗口时间升序（`09:30-09:40` → `09:40-09:50` → ...）

**次排序**：窗口内按 `first_appear_time` 升序（先出现的在前）

---

## 五、后端实现要点

### 5.1 窗口聚合逻辑

```python
# 伪代码
def aggregate_by_window(results, window_minutes=10):
    """按10分钟窗口聚合"""
    windows = {}
    
    for tick in results:
        window_key = get_window_key(tick['time'], window_minutes)  # "14:20-14:30"
        
        for stock in tick['stocks']:
            pair_key = (stock['code'], stock['bond_code'])
            
            if pair_key not in windows[window_key]:
                windows[window_key][pair_key] = {
                    'first_appear_time': tick['time'],
                    'first_change_pct': stock['change_pct'],
                    'max_change_pct': stock['change_pct'],
                    'max_change_time': tick['time'],
                    'appear_count': 1,
                    'details': [{
                        'time': tick['time'],
                        'change_pct': stock['change_pct'],
                        'window_count': stock['window_count'],
                        'is_first': True
                    }]
                }
            else:
                w = windows[window_key][pair_key]
                w['appear_count'] += 1
                w['details'].append({...})
                
                # 更新最高
                if stock['change_pct'] > w['max_change_pct']:
                    w['max_change_pct'] = stock['change_pct']
                    w['max_change_time'] = tick['time']
    
    # 计算派生字段
    for window in windows.values():
        for pair in window.values():
            pair['time_to_max_seconds'] = calculate_seconds(
                pair['first_appear_time'], 
                pair['max_change_time']
            )
            pair['time_to_max_display'] = format_duration(
                pair['time_to_max_seconds']
            )
            pair['gain_to_max'] = round(
                pair['max_change_pct'] - pair['first_change_pct'], 
                2
            )
    
    return windows
```

### 5.2 响应格式

```json
{
  "code": 0,
  "data": {
    "windows": ["09:30-09:40", "09:40-09:50", ...],
    "summary": [
      {
        "window": "14:20-14:30",
        "window_start": "14:20:00",
        "window_end": "14:29:57",
        "pair_count": 3,
        "pairs": [...]
      }
    ]
  }
}
```

---

## 六、前端实现要点

### 6.1 组件结构

```
BacktraceResults
├── WindowFilter (窗口筛选器)
├── SummaryTable (汇总表格)
│   └── SummaryRow
│       └── PairDetail (展开明细)
│           └── Timeline (时间轴)
│           └── DetailTable (明细表)
└── StatisticsPanel (统计面板)
```

### 6.2 交互逻辑

1. **窗口筛选**：点击窗口按钮，表格只显示该窗口数据
2. **展开/收起**：点击行或按钮，展开该行的时间轴和明细表
3. **排序**：表头可点击排序（窗口、次数、时间差、涨幅差）

---

## 七、实施步骤

1. [ ] 后端：`backtrace.py` 添加窗口聚合逻辑
2. [ ] 后端：计算派生字段（时间差、涨幅差）
3. [ ] 前端：修改 `displayResults` 接收新格式
4. [ ] 前端：创建 `SummaryTable` 组件
5. [ ] 前端：创建 `PairDetail` 展开组件（时间轴+明细表）
6. [ ] 前端：添加窗口筛选器
7. [ ] 联调测试

---

## 八、需你最终确认

1. 展示设计是否满意？（汇总表格 + 时间轴展开）
2. 表格列是否足够？需要添加/删除列吗？
3. 时间差和涨幅差的颜色阈值是否需要调整？
4. 是否需要导出功能（CSV/Excel）？

---

**等待最终审核**
