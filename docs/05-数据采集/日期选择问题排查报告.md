# 日期选择问题排查报告

> 时间：2026-04-28 01:14  
> 对比版本：ee58bae (回退点) vs 82beda3 (当前)

---

## 排查结果

### 1. JavaScript语法检查
- [OK] Promise.all 语法正确
- [OK] 解构赋值语法正确  
- [OK] 未发现明显的语法问题

### 2. 日期选择器相关代码对比

#### 初始化代码（未修改）
```javascript
(function initDatePicker() {
    const p = document.getElementById('date-picker');
    const d = new Date();
    p.value = d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');
    p.addEventListener('change', () => { 
        _isLive = false; 
        updateRedListForSelectedDate();
        loadTimestamps(); 
        loadAllData(); 
    });
})();
```

**结论**：日期选择器的初始化代码在两个版本中**完全一致**，没有修改。

#### loadAllData函数（未修改）
```javascript
function loadAllData(timeStr) {
    loadMarketStats(timeStr);
    loadStockRanking(timeStr);
    loadBondRanking(timeStr);
    loadIndustryRanking(timeStr);
    loadCombineSignal(timeStr);
    updateLastUpdateTime();
}
```

**结论**：`loadAllData` 函数在两个版本中**完全一致**。

### 3. 实际修改的代码

#### 修改1：loadBondRanking函数
```javascript
// 修改前
async function loadBondRanking(timeStr) {
    showRefreshIndicator('bond');
    try { 
        const r = await (await fetch(buildRankUrl('bond', timeStr))).json(); 
        // ...
    } catch(e){}
}

// 修改后
async function loadBondRanking(timeStr) {
    showRefreshIndicator('bond');
    try {
        const [stockRes, bondRes] = await Promise.all([
            fetch(buildRankUrl('stock', timeStr)),
            fetch(buildRankUrl('bond', timeStr))
        ]);
        // ...
    } catch(e){}
}
```

#### 修改2：renderRanking函数（债券代码列）
```javascript
// 新增联动标记
const linkBadge = (isBondRanking && item.is_stock_linked) ? '<span class="link-badge">★</span>' : '';
rowHtml = `<tr${trAttr}><td class="${rc}">${curRank}</td><td class="code">${linkBadge}${code}</td>...`;
```

#### 修改3：CSS样式
```css
.link-badge {
    display: inline-block;
    background: linear-gradient(135deg, #ffd700 0%, #ffaa00 100%);
    color: #fff;
    font-size: 10px;
    padding: 1px 3px;
    border-radius: 3px;
    margin-right: 3px;
    font-weight: bold;
}
```

---

## 结论

### 是否由刚才的改动引起？
**否。**

### 证据
1. 日期选择器的初始化代码 `initDatePicker` **完全没有修改**
2. `loadAllData` 函数 **完全没有修改**
3. 所有修改集中在 `loadBondRanking` 和 `renderRanking` 函数，与日期选择无关
4. JavaScript语法检查通过，无语法错误

### 可能的原因
1. **浏览器缓存** - 尝试清除浏览器缓存后刷新
2. **后端服务问题** - 检查Flask服务是否正常重启
3. **其他代码问题** - 需要查看浏览器控制台错误信息

---

## 建议的排查步骤

1. **浏览器控制台检查**
   - 按F12打开开发者工具
   - 查看Console是否有JavaScript错误
   - 查看Network面板，看日期选择后是否有请求发出

2. **清除缓存**
   - Ctrl+F5 强制刷新
   - 或清除浏览器缓存后重试

3. **检查后端日志**
   - 查看Flask服务是否有错误日志
   - 确认服务是否正常重启

4. **回退验证**
   - 如需要，可以回退到 `ee58bae` 验证问题是否依然存在

---

**结论**：根据代码对比分析，日期选择问题**不是**由刚才的改动引起的。建议按上述步骤进一步排查。
