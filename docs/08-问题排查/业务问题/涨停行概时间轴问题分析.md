# 涨停行概时间轴问题分析

## 问题描述
用户反馈"涨停表查询错误"，需要分析具体问题并给出解决方案。

## 当前代码逻辑分析

### 1. 获取时间戳列表 (`get_ztb_timestamps`)

**当前实现：**
```python
redis_key = f"monitor_gp_apqd_{date_str}:timestamps"
timestamps = redis_client.lrange(redis_key, 0, -1)
```

**问题分析：**
- ✅ 从Redis获取时间戳列表，逻辑正确
- ⚠️ `monitor_gp_apqd`表只存Redis，不存MySQL（已确认）
- ⚠️ 如果Redis中没有数据，会返回空列表

### 2. 获取涨停快照 (`get_ztb_snapshot`)

**当前实现：**
```python
# 1. 先尝试从Redis获取
redis_key = f"monitor_gp_sssj_{date_str}:{time}"
data = redis_client.get(redis_key)

# 2. 从MySQL获取
table_name = f"monitor_gp_sssj_{date_str}"
sql = f"SELECT * FROM {table_name} WHERE time_str = '{time}' AND is_zt = 1"
```

**问题分析：**
- ✅ 先Redis后MySQL，逻辑正确
- ❓ MySQL查询使用`time_str`列，需要确认实际列名
- ❓ 需要确认`is_zt`列是否存在

### 3. 筛选涨停股票 (`filter_ztb_snapshot`)

**当前实现：**
```python
# 1. 从Redis获取
data = redis_client.get(redis_key)

# 2. 从MySQL获取
table_name = f"monitor_gp_sssj_{date_str}"
sql = f"SELECT * FROM {table_name} WHERE time_str = '{time}' AND is_zt = 1"
```

**问题分析：**
- 与`get_ztb_snapshot`相同的问题

---

## 需要确认的信息

### 1. MySQL表结构确认

需要确认`monitor_gp_sssj_20260429`表的列名：
- 时间列是`time`还是`time_str`？
- 涨停标记列是`is_zt`还是其他名称？

### 2. Redis数据确认

需要确认Redis中是否有数据：
- `monitor_gp_apqd_20260429:timestamps` 是否存在？
- `monitor_gp_sssj_20260429:14:30:00` 是否存在？

---

## 解决方案

### 方案A：修复MySQL列名

如果MySQL表的实际列名与代码不符，需要修改：

```python
# 修改前
sql = f"SELECT * FROM {table_name} WHERE time_str = '{time}' AND is_zt = 1"

# 修改后（假设实际列名是'time'）
sql = f"SELECT * FROM {table_name} WHERE time = '{time}' AND is_zt = 1"
```

### 方案B：增强错误处理和日志

添加更详细的日志，帮助定位问题：

```python
def get_ztb_snapshot(date: str, time: str) -> Dict:
    logger.info(f"开始获取涨停快照: date={date}, time={time}")
    
    # 1. 尝试Redis
    redis_key = f"monitor_gp_sssj_{date_str}:{time}"
    logger.debug(f"Redis key: {redis_key}")
    
    # 2. 尝试MySQL
    logger.debug(f"MySQL SQL: {sql}")
```

### 方案C：统一使用Redis（如果MySQL表结构不确定）

如果MySQL表结构复杂或不确定，可以只使用Redis：

```python
def get_ztb_snapshot(date: str, time: str) -> Dict:
    # 只从Redis获取
    redis_key = f"monitor_gp_sssj_{date_str}:{time}"
    data = redis_client.get(redis_key)
    if data:
        df = pd.DataFrame(json.loads(data))
        # 筛选is_zt=1
        df = df[df['is_zt'] == 1]
        return _analyze_snapshot(df)
    return {'total_count': 0, 'industries': [], 'concepts': []}
```

---

## 建议实施步骤

1. **确认MySQL表结构**
   - 查看`monitor_gp_sssj_20260429`表的实际列名
   - 确认时间列和涨停标记列的名称

2. **确认Redis数据**
   - 检查Redis中是否有时间戳列表
   - 检查Redis中是否有具体时间点的数据

3. **根据确认结果修复代码**
   - 如果列名不匹配，修改SQL语句
   - 如果Redis没有数据，检查数据采集流程

4. **添加详细日志**
   - 帮助后续排查问题

---

## 需要用户确认

请提供以下信息：

1. **MySQL表结构**
   ```sql
   DESCRIBE monitor_gp_sssj_20260429;
   ```

2. **Redis数据检查**
   ```bash
   redis-cli keys "monitor_gp_apqd_20260429*"
   redis-cli keys "monitor_gp_sssj_20260429*" | head -5
   ```

3. **具体错误信息**
   - 后端完整的错误日志
   - 前端的具体报错内容

---

等待用户确认后实施修复。
