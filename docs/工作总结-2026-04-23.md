# 2026-04-23 工作总结

> 日期: 2026-04-23
> 工作内容: 涨停行概选股优化、前端展示优化、Bug修复、系统配置优化

---

## 一、涨停行概选股性能优化

### 1.1 问题诊断
- **问题**: 涨停行概选股查询慢，无Redis缓存
- **根因**: 代码直接从MySQL查询，未使用Redis；内存缓存仅用于宽表数据

### 1.2 实施方案
1. **索引优化**: 增加 `is_zt` 和 `is_zt_time` 复合索引
2. **Redis优先**: 新增三个Redis查询函数
3. **MySQL回退**: Redis失败时回退到MySQL查询

### 1.3 修改文件
- `src/gs2026/monitor/table_index_manager.py` - 增加索引
- `src/gs2026/utils/redis_util.py` - 新增Redis查询函数
- `src/gs2026/dashboard2/services/stock_picker_service.py` - 使用Redis优先

### 1.4 Git提交
- `eee5122` feat: 涨停行概选股性能优化 - Redis优先 + 索引优化

---

## 二、涨停行概选股Bug修复

### 2.1 stock_code类型不匹配
- **问题**: Redis中stock_code为int64，缓存为字符串，导致匹配失败
- **修复**: `get_zt_stocks_from_redis` 中转换stock_code为字符串并补零
- **提交**: `4f08864`

### 2.2 股票筛选问题
- **问题**: 股票筛选同时匹配名称和代码，LIKE语句%被pymysql当参数占位符
- **修复**: 使用 `LIKE '%%{value}%%'` 转义%
- **提交**: `50b6741`

### 2.3 查询结果显示问题
- **问题**: 涨停行概选股查询结果显示异常
- **修复**: 修改 `renderResults` 函数支持多面板复用
- **提交**: `2d0afd3`

### 2.4 删除6个标签限制
- **问题**: 涨停行概选股限制6个标签
- **修复**: 删除标签数量限制
- **提交**: `9a7b9f0`

---

## 三、前端展示优化

### 3.1 所属行概展示优化
- **问题**: 概念标签过多，列表很长
- **优化**:
  - 每行5个标签，超出折叠
  - 添加悬浮提示显示完整名称
  - 标签添加细边框
  - 展开/收起按钮优化
- **提交**: `fafb9b2`, `afe93f2`, `35f5024`, `541a1dc`

### 3.2 筛选器位置调整
- **优化**: 将筛选器（仅显示有债券、股票涨跌幅、债券涨跌幅）移到查询按钮左侧
- **提交**: `fafb9b2`

### 3.3 页面标题动态更新
- **修复**: 切换Tab时更新 `document.title`
- **提交**: `8b36d13`

---

## 四、上攻排行Bug修复

### 4.1 时间轴+排序数据不一致
- **问题**: 点击时间轴后，再点击排序，数据回到最新时间
- **根因**: `loadStockRankingAtTime` 未保存数据到 `_rankRawData`
- **修复**: 添加 `_rankRawData['stock-ranking'] = r.data`
- **提交**: `be2cbec`

---

## 五、公告分析Bug修复

### 5.1 搜索框支持股票名称
- **问题**: 输入股票名称查询无结果
- **修复**: 判断输入类型（数字/文字），分别发送stock_code或stock_name参数
- **提交**: `9096165`

### 5.2 LIKE转义问题
- **问题**: stock_name模糊查询时%被pymysql误解析
- **修复**: 使用 `LIKE '%%{stock_name}%%'` 转义
- **提交**: `f866331`

---

## 六、系统配置优化

### 6.1 Redis连接池优化
- **优化项**:
  - `socket_connect_timeout`: 3s → 10s
  - `socket_timeout`: 5s → 30s
  - 增加 `retry_on_error` 配置
  - 增加TCP保活配置
- **提交**: `67e824b`

---

## 七、数据采集方案设计

### 7.1 Baostock替代方案
- **问题**: baostock登录失败，无法采集data_gpsj_day数据
- **方案**: 使用AKShare/AData替代，或从Monitor表同步
- **文档**: `docs/05-数据采集/Baostock替代方案-20260423.md`

### 7.2 TickFlow方案
- **分析**: adata.list_market_current可获取实时数据，但缺少OHLC
- **方案**: 从monitor_gp_sssj表同步日数据
- **文档**: `docs/05-数据采集/TickFlow获取日数据方案.md`

---

## 八、Git提交汇总

| Commit | 说明 |
|--------|------|
| `eee5122` | 涨停行概选股性能优化 - Redis优先 + 索引优化 |
| `4f08864` | 修复stock_code类型不匹配问题 |
| `fafb9b2` | 优化涨停行概选股展示 |
| `afe93f2` | 优化所属行概标签展示样式 |
| `67e824b` | 优化Redis连接池配置为稳定级参数 |
| `8b36d13` | 修复涨停行概选股三个问题 |
| `35f5024` | 移除收起按钮，只保留数字展开按钮 |
| `541a1dc` | 展开后按钮移动到所有标签最后面 |
| `be2cbec` | 修复上攻排行时间轴+排序数据不一致问题 |
| `9096165` | 公告分析搜索框支持股票名称模糊查询 |
| `f866331` | 修复公告分析stock_name模糊查询LIKE转义问题 |

---

## 九、待办事项

- [ ] 提交未提交的修改到git
- [ ] 合并到master分支
- [ ] 实施Baostock替代方案
- [ ] 测试AKShare/AData数据源

---

**记录人**: AI Assistant
**日期**: 2026-04-23
