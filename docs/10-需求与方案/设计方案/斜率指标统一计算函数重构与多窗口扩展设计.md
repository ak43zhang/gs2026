# 斜率指标统一计算函数重构与多窗口扩展设计

> 版本: v2.0  
> 日期: 2026-07-18  
> 状态: 已实施（渐进式计算）

---

## 一、背景与问题

### 1.1 当前问题

1. **代码重复**: `monitor_bond.py`（实时）和 `backfill_unified.py`（回填）各自独立实现了斜率计算逻辑，修改一处不会同步另一处
2. **指标不足**: 只有2分钟窗口斜率，无法区分"真上涨"和"下跌中的短暂反弹"
3. **早盘无数据**: 15分钟斜率需等15分钟数据，09:45前无值，影响早盘策略

### 1.2 解决方案

| 问题 | 方案 |
|------|------|
| 代码重复 | 统一计算函数，实时和回填共用 |
| 指标不足 | 新增5分钟/15分钟斜率 |
| 早盘无数据 | **渐进式计算**：数据不足时用现有全部数据 |

---

## 二、指标设计

### 2.1 渐进式计算原理

**核心逻辑**：
```
数据点 >= 要求点数（如20点）:
    → 基于完整窗口计算（如15分钟）
数据点 < 要求点数 但 >= 最小点数（如8点）:
    → 基于现有全部数据计算（渐进式）
数据点 < 最小点数:
    → 返回None
```

**实际效果**：
| 时间 | 数据点 | 15分钟斜率 | 计算窗口 | 说明 |
|------|--------|------------|----------|------|
| 09:30:09 | 3点 | None | - | 数据不足 |
| 09:30:24 | 8点 | ✅ 0.0234 | 24秒 | **渐进式，早盘可用** |
| 09:35:00 | 100点 | ✅ 0.0189 | 5分钟 | **渐进式，基于5分钟** |
| 09:45:00 | 300点 | ✅ 0.0156 | 15分钟 | **完整窗口** |

> **关键洞察**：时间本身就是质量标记。09:35的斜率基于5分钟数据，策略层自然会更谨慎使用。

### 2.2 完整指标清单

#### 个券指标

| 字段名 | 中文名 | 窗口 | half_life | 最小点数 | 说明 |
|--------|--------|------|-----------|----------|------|
| `weighted_slope_2m` | 加权斜率(2m) | 2分钟 | 30s | 5 | 短期趋势(严格窗口) |
| `weighted_slope_5m` | 加权斜率(5m) | 渐进 | 60s | 5 | 中期趋势 **渐进式** |
| `weighted_slope_15m` | 加权斜率(15m) | 渐进 | 180s | 8 | 中长期趋势 **渐进式** |
| `change_1m_pct` | 1分钟涨幅% | 1分钟 | - | 2 | 即时涨幅 |
| `price_acceleration` | 价格加速度 | - | - | - | 斜率差分 |

#### 大盘指标

| 字段名 | 中文名 | 窗口 | half_life | 最小点数 | 说明 |
|--------|--------|------|-----------|----------|------|
| `mkt_weighted_slope_2m` | 大盘加权斜率(2m) | 2分钟 | 30s | 5 | 短期大盘(严格窗口) |
| `mkt_weighted_slope_5m` | 大盘加权斜率(5m) | 渐进 | 60s | 5 | 中期大盘 **渐进式** |
| `mkt_weighted_slope_15m` | 大盘加权斜率(15m) | 渐进 | 180s | 8 | 中长期大盘 **渐进式** |
| `mkt_change_1m_pct` | 大盘1分钟涨幅% | 1分钟 | - | 2 | 即时大盘涨幅 |
| `mkt_price_acceleration` | 大盘价格加速度 | - | - | - | 大盘斜率差分 |

> **渐进式说明**：5分钟/15分钟斜率采用渐进式计算。数据充足时用完整窗口，数据不足时用现有全部数据（最少5/8个点）。时间本身就是质量标记。

### 2.2 参数设计依据

| 窗口 | half_life | 有效响应延迟 | 设计意图 |
|------|-----------|-------------|----------|
| 2分钟 | 30s | ~30秒 | 快速捕捉短期动量变化 |
| 5分钟 | 60s | ~1分钟 | 中期趋势确认，过滤1-2分钟噪音 |
| 15分钟 | 180s | ~3分钟 | 大方向判断，过滤5分钟级别波动 |

> **half_life的含义**: 距当前时间 half_life 秒之前的数据点权重衰减到50%。  
> half_life越大 → 越多历史数据参与 → 越稳定但越滞后

### 2.3 多窗口配合逻辑

```
15分钟斜率 > 0  →  大方向上涨（过滤下跌中的反弹）
 + 5分钟斜率 > 0  →  中期确认上涨
  + 2分钟斜率 > 0.1 →  短期有强动量 → 入场!

15分钟斜率 > 0 但 2分钟斜率 < 0  →  大方向上涨，短期回调 → 不入场，等待
15分钟斜率 < 0 且 2分钟斜率 > 0  →  下跌中反弹 → 过滤！
```

---

## 三、架构设计

### 3.1 统一函数设计

在 `monitor_bond.py` 中定义两个统一计算函数：

```python
def calc_bond_ext_indicators(price_history, prev_slope=None):
    """
    【统一个券扩展指标计算函数】
    
    参数:
        price_history: deque/list of (timestamp_float, price_float)
                       时间升序排列的价格序列
                       调用方需确保数据窗口足够(建议保留15分钟以上)
        prev_slope: float or None
                    上一tick的 weighted_slope_2m，用于计算 price_acceleration
                    首次调用传 None
    
    返回: dict
        {
            'weighted_slope_2m': float | None,    # 加权斜率(2m)
            'weighted_slope_5m': float | None,    # 加权斜率(5m) - 渐进式
            'weighted_slope_15m': float | None,   # 加权斜率(15m) - 渐进式
            'change_1m_pct': float | None,        # 1分钟涨幅%
            'price_acceleration': float | None,   # 价格加速度
        }
    
    计算逻辑:
        1. 从 price_history 尾部取最近 N 秒数据
        2. 调用 _calc_weighted_slope 计算各窗口斜率
           - 2分钟斜率: 严格窗口120秒，需5个点
           - 5分钟斜率: 渐进式，需5个点（约15秒即可）
           - 15分钟斜率: 渐进式，需8个点（约24秒即可）
        3. 计算 change_1m_pct = (当前价 - 60秒前价) / 60秒前价 × 100
        4. 计算 acceleration = 当前slope_2m - prev_slope
    
    渐进式说明:
        - 5分钟/15分钟斜率采用渐进式计算
        - 数据充足时用完整窗口，数据不足时用现有全部数据
        - 时间本身就是质量标记，策略层可用 time >= '09:45:00' 判断
    
    调用方:
        - monitor_bond.py 实时计算 (每tick ~3秒调用一次)
        - backfill_unified.py 历史回填 (逐行调用)
    """

def calc_mkt_ext_indicators(mkt_price_history, prev_slope=None):
    """
    【统一大盘扩展指标计算函数】
    
    参数: 同上
    
    返回: dict
        {
            'mkt_weighted_slope_2m': float | None,    # 大盘加权斜率(2m)
            'mkt_weighted_slope_5m': float | None,    # 大盘加权斜率(5m) - 渐进式
            'mkt_weighted_slope_15m': float | None,   # 大盘加权斜率(15m) - 渐进式
            'mkt_change_1m_pct': float | None,        # 大盘1分钟涨幅%
            'mkt_price_acceleration': float | None,   # 大盘价格加速度
        }
    
    调用方: 同上
    """
```

### 3.2 调用关系

```
monitor_bond.py
├── _calc_weighted_slope(prices, times, half_life)  ← 底层EWLR算法
├── calc_bond_ext_indicators(history, prev)          ← 统一个券计算
├── calc_mkt_ext_indicators(history, prev)           ← 统一大盘计算
│
├── [实时] compute_ext_indicators(df_now)
│       维护 _price_cache (per-bond deque)
│       调用 calc_bond_ext_indicators()
│
└── [实时] compute_mkt_ext_indicators(df_now)
        维护 _mkt_price_cache (单个deque)
        调用 calc_mkt_ext_indicators()

backfill_unified.py
├── from gs2026.monitor.monitor_bond import (
│       calc_bond_ext_indicators,
│       calc_mkt_ext_indicators
│   )
├── backfill_ext_indicators():  构建缓存 → 调用 calc_bond_ext_indicators()
└── backfill_mkt_ext_indicators(): 构建缓存 → 调用 calc_mkt_ext_indicators()
```

### 3.3 缓存设计

| 缓存 | 作用 | maxlen | 保留时间 | 内存 |
|------|------|--------|----------|------|
| `_price_cache[code]` | 个券价格序列 | 300 | 900s(15min) | 400×300×16B = **1.9MB** |
| `_mkt_price_cache` | 大盘均价序列 | 300 | 900s(15min) | 300×16B = **5KB** |

> 较之前(maxlen=60, 150s)，个券缓存扩大5倍，内存从0.4MB增至1.9MB，完全可接受

---

## 四、前端字段对照表

### 4.1 回测条件可用字段

| 字段名 | 前端中文名 | 典型范围 | 回测条件示例 |
|--------|-----------|----------|-------------|
| `weighted_slope_2m` | 加权斜率(2m) | -0.3 ~ +0.3 | > 0.1 (短期强势) |
| `weighted_slope_5m` | 加权斜率(5m) | -0.2 ~ +0.2 | > 0.05 (中期上涨，渐进式) |
| `weighted_slope_15m` | 加权斜率(15m) | -0.15 ~ +0.15 | > 0 (大方向向上，渐进式) |
| `change_1m_pct` | 1分钟涨幅% | -1.0 ~ +1.0 | > 0.1 |
| `price_acceleration` | 价格加速度 | -0.1 ~ +0.1 | > 0 (加速中) |
| `mkt_weighted_slope_2m` | 大盘加权斜率(2m) | -0.2 ~ +0.2 | > 0.03 |
| `mkt_weighted_slope_5m` | 大盘加权斜率(5m) | -0.15 ~ +0.15 | > 0.02 (渐进式) |
| `mkt_weighted_slope_15m` | 大盘加权斜率(15m) | -0.1 ~ +0.1 | > 0 (大盘方向向上，渐进式) |
| `mkt_change_1m_pct` | 大盘1分钟涨幅% | -0.5 ~ +0.5 | > -0.1 |
| `mkt_price_acceleration` | 大盘价格加速度 | -0.05 ~ +0.05 | > 0 |

> **渐进式说明**：5分钟/15分钟斜率采用渐进式计算。09:30:24即可有15分钟斜率值（基于24秒数据），09:45后自动过渡到完整15分钟窗口。

### 4.2 常用策略组合

| 策略 | 条件 | 效果 |
|------|------|------|
| 大方向+短期动量 | `mkt_weighted_slope_15m > 0` AND `weighted_slope_2m > 0.1` | 只在大盘上涨中捕捉强势票 |
| 三窗口共振 | `weighted_slope_15m > 0` AND `weighted_slope_5m > 0.05` AND `weighted_slope_2m > 0.1` | 个股三级趋势全部向上 |
| 过滤反弹 | `mkt_weighted_slope_15m > 0` AND `mkt_weighted_slope_5m > 0` | 确保不是下跌中的反弹 |
| 逆势强票 | `mkt_weighted_slope_15m < -0.02` AND `weighted_slope_5m > 0.05` | 大盘跌但个股依然上涨 |
| 加速突破 | `weighted_slope_5m > 0.05` AND `price_acceleration > 0.02` | 中期上涨且正在加速 |
| **早盘策略** | `mkt_weighted_slope_15m > 0` AND `time >= '09:30:30'` | 09:30:24即可开始捕捉 |

### 4.3 数据质量判断

策略层可通过**时间**判断数据质量：

| 时间 | 15分钟斜率计算窗口 | 建议 |
|------|-------------------|------|
| 09:30:00 - 09:30:24 | None（不足8点） | 不使用 |
| 09:30:24 - 09:45:00 | 24秒 - 15分钟（渐进） | 谨慎使用，配合时间条件 |
| 09:45:00 以后 | 完整15分钟 | 标准使用 |
| 保守策略 | 只用严格版 | 确保数据质量 |

---

## 五、数据有效性

### 5.1 Warmup 时间

| 指标 | 最早有效时间 | 说明 |
|------|-------------|------|
| `weighted_slope_2m` | 开盘后~15秒(5个点) | 很快生效 |
| `weighted_slope_5m` | 开盘后~30秒(10个点) | 较快生效 |
| `weighted_slope_5m_prog` | 开盘后~15秒(5个点) | **早盘即可用** |
| `weighted_slope_15m` | 开盘后~60秒(20个点) | 约1分钟后有值 |
| `weighted_slope_15m_prog` | 开盘后~24秒(8个点) | **早盘即可用** |

> **分层设计优势**：
> - 严格版：确保数据质量，用于标准策略
> - 渐进版：09:30:24即可开始计算，用于早盘策略
> - 时间本身就是质量标记，无需额外字段

### 5.2 渐进版 vs 严格版数值对比

以某天09:35:00为例（假设已运行5分钟）：

| 字段 | 计算窗口 | 数值示例 | 说明 |
|------|----------|----------|------|
| `weighted_slope_15m` | None | None | 数据不足，返回None |
| `weighted_slope_15m_prog` | 5分钟 | 0.0234 | 基于5分钟数据计算 |

09:45:00（运行15分钟后）：

| 字段 | 计算窗口 | 数值示例 | 说明 |
|------|----------|----------|------|
| `weighted_slope_15m` | 15分钟 | 0.0189 | 完整15分钟数据 |
| `weighted_slope_15m_prog` | 15分钟 | 0.0189 | 两者一致 |

### 5.3 回填覆盖

| 场景 | 是否自动覆盖 |
|------|-------------|
| 新增指标后跑 backfill | ✅ 自动填充所有新字段 |
| 历史数据每天前60秒 | ⚠️ 15m斜率精度较低，正常现象；渐进版可用 |
| 跨天无连续性 | ✅ 每天独立计算，不跨天 |

---

## 六、实施计划

### 6.1 改动文件清单

| 文件 | 改动内容 | 工作量 |
|------|----------|--------|
| `monitor_bond.py` | 新增 `calc_bond_ext_indicators` + `calc_mkt_ext_indicators`；现有计算逻辑改为调用；缓存扩展到900s；**新增渐进版字段** | 1.5小时 |
| `backfill_unified.py` | 删除重复函数，改为 import 调用统一函数 | 30分钟 |
| `monitor.html` | FIELD_OPTIONS 添加**8个**新字段中文名（严格版4个+渐进版4个） | 10分钟 |
| 本文档 | 已更新分层设计 | - |

### 6.2 验证步骤

1. 修改后启动 `monitor_bond.py`，观察日志确认新字段计算正常
2. 在量化回测页面设置条件 `mkt_weighted_slope_15m > 0` 验证能筛选
3. **验证渐进版**：09:30:24检查 `weighted_slope_15m_prog` 是否有值（严格版应为None）
4. 运行 `backfill_unified.py` 回填1天数据，验证历史数据含新字段
5. 对比新旧字段值确保 `weighted_slope_2m` 等已有字段计算结果不变

---

## 七、风险评估

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 重构后已有指标数值变化 | 低 | 高 | 保持算法完全一致，单元测试对比 |
| 缓存内存增大 | 确定 | 低 | 从0.4MB→1.9MB，可接受 |
| 回填耗时增加 | 确定 | 低 | 多算4个斜率，每天多~20秒 |
| 渐进版值不稳定被误用 | 中 | 中 | 文档说明，字段名带`_prog`后缀天然警示 |
| 字段数量翻倍增加复杂度 | 确定 | 低 | 前端分组展示，策略按需选择 |

---

*文档结束*
