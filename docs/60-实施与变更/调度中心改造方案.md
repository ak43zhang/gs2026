# 调度中心改造方案（持续更新文档）

> **文档类型**: 单文件多版本沿革  
> **维护方式**: 每次更新在此文件追加新版本，保留历史版本  
> **最新版本**: v4.0

---

## 📌 版本沿革

| 版本 | 日期 | 主要内容 |
|------|------|----------|
| v1.0 | 2026-07-29 | Windows服务化基础方案 |
| v2.0 | 2026-07-29 | 补充调度链设计、完整数据流 |
| v3.0 | 2026-07-29 | 原理深度解析（核心）+ 结合当前代码的完整方案 + 三种调度链模式 + 参数配置详解 |
| **v4.0** | **2026-07-29** | **交易日动态调度（基于data_jyrl表）+ 前端执行状态可视化（判断执行/成功失败/阶段进度）** |

---

# ========================================
# v3.0（当前版本）2026-07-29
# ========================================

## 一、【核心原理】前端设置如何生效？（重点解答疑惑）

### 1.1 当前代码的致命问题

**你的疑惑**："前端设置后，读MySQL的是Windows独立运行的服务吗？还是只有app.py执行时MySQL配置才生效？"

**答案**：**当前代码架构下，只有app.py执行时MySQL配置才生效！** 这正是需要改造的核心问题。

### 1.2 为什么？（代码级证据）

#### 证据1：调度器是单例，绑定在进程内

```python
# scheduler_service.py 行814
scheduler_service = SchedulerService()  # 全局单例

# 关键：这个单例是"谁import它，就在谁的进程里"
# - app.py import → 单例在app.py进程
# - Windows服务 import → 单例在服务进程
# 两个进程的单例是【完全独立】的两个实例！
```

#### 证据2：APScheduler用的是内存JobStore（无持久化）

```python
# scheduler_service.py 行92-99
self.scheduler = BackgroundScheduler(
    timezone='Asia/Shanghai',
    job_defaults={...}
    # ❌ 没有配置 jobstore！默认使用 MemoryJobStore
)
```

**MemoryJobStore的含义**：
- 任务调度信息**只存在内存**
- 进程重启 → 内存清空 → 调度信息丢失
- 只能靠 `_load_jobs_from_db()` 重新从MySQL加载

#### 证据3：前端add_job的实际行为

```python
# scheduler_service.py 行652-691
def add_job(self, job_config: Dict) -> str:
    # 1. 写入MySQL（这一步持久化了）
    execute_update(sql, (...))
    
    # 2. 添加到【当前进程的】APScheduler
    if job_config.get('status') == 'enabled':
        self._add_job_to_scheduler(job[0])  # ← 关键！
    
    return job_id
```

**关键问题**：
- `self._add_job_to_scheduler()` 添加到的是**app.py进程内的scheduler**
- 因为前端API请求由**app.py处理**，所以`self`是app.py进程的单例
- **Windows服务进程的scheduler对此一无所知！**

### 1.3 当前架构的数据流（问题所在）

```
前端创建任务
    │
    ▼ POST /api/scheduler/jobs （app.py处理）
┌─────────────────────────────────────┐
│  app.py 进程                         │
│  ┌─────────────────────────────────┐ │
│  │ scheduler_service (单例A)       │ │
│  │ - 写入MySQL ✅                  │ │
│  │ - add到自己的APScheduler ✅     │ │  ← 只有app.py进程知道
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘

    如果同时有Windows服务进程:
┌─────────────────────────────────────┐
│  Windows服务 进程                    │
│  ┌─────────────────────────────────┐ │
│  │ scheduler_service (单例B)       │ │
│  │ - APScheduler ❌ 不知道新任务   │ │  ← 完全独立，不知道app.py加的任务
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**结论**：
- 当前：前端设置 → app.py进程的scheduler生效 → **app.py停止则调度停止**
- Windows服务如果直接跑，**看不到前端通过app.py添加的任务**（除非重启重新加载MySQL）

### 1.4 改造后的正确架构（解决方案）

```
前端创建任务
    │
    ▼ POST /api/scheduler/jobs （app.py处理）
┌─────────────────────────────────────┐
│  app.py 进程（只负责写MySQL）        │
│  - 写入MySQL ✅                      │
│  - 不再add到自己的scheduler ❌       │  ← 改造：app.py不调度
└─────────────────────────────────────┘
    │
    ▼ 写入 scheduler_jobs + 变更通知
┌─────────────────────────────────────┐
│  MySQL（唯一数据源）                 │
│  - scheduler_jobs（任务配置）        │
│  - scheduler_reload_signal（变更信号）│
└─────────────────────────────────────┘
    │
    ▼ 轮询检测（每30秒）
┌─────────────────────────────────────┐
│  Windows服务 进程（唯一调度者）      │
│  ┌─────────────────────────────────┐ │
│  │ scheduler_service               │ │
│  │ - 轮询检测MySQL变更 ✅          │ │
│  │ - 发现新任务→加载到APScheduler  │ │
│  │ - 按触发器执行 ✅               │ │
│  │ - app.py停止也不影响 ✅         │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**核心改造点**：
1. **app.py不再调度**：只写MySQL，移除 `_add_job_to_scheduler` 调用
2. **Windows服务唯一调度**：独立进程运行APScheduler
3. **MySQL变更检测**：服务轮询检测新任务/修改/删除
4. **可选：SQLAlchemyJobStore**：用MySQL做jobstore，实现跨进程共享

---

## 二、两种实现方案对比

### 方案X：轮询检测（推荐，简单可靠）

```python
# Windows服务轮询MySQL，发现变更就重载
class SchedulerService:
    def start(self):
        self.scheduler.start()
        self._load_jobs_from_db()  # 初次加载
        
        # 添加轮询任务，每30秒检查变更
        self.scheduler.add_job(
            func=self._sync_jobs_from_db,
            trigger='interval',
            seconds=30,
            id='__sync_jobs',
            replace_existing=True
        )
    
    def _sync_jobs_from_db(self):
        """同步MySQL任务到调度器"""
        # 1. 查询所有enabled任务
        db_jobs = execute_query("SELECT job_id, updated_at FROM scheduler_jobs WHERE status='enabled'")
        db_job_ids = {j['job_id'] for j in db_jobs}
        
        # 2. 当前调度器中的任务
        scheduler_job_ids = {j.id for j in self.scheduler.get_jobs() if not j.id.startswith('__')}
        
        # 3. 新增的任务（DB有，调度器没有）
        for job in db_jobs:
            if job['job_id'] not in scheduler_job_ids:
                self._reload_single_job(job['job_id'])
            elif self._is_job_updated(job['job_id'], job['updated_at']):
                self._reload_single_job(job['job_id'])
        
        # 4. 删除的任务（调度器有，DB没有或disabled）
        for job_id in scheduler_job_ids - db_job_ids:
            self.scheduler.remove_job(job_id)
```

**优点**：
- 简单可靠，无需额外组件
- 跨进程完全解耦
- app.py和服务完全独立

**缺点**：
- 最多30秒延迟（可调整）

### 方案Y：SQLAlchemyJobStore（高级，实时）

```python
# 用MySQL做APScheduler的jobstore，天然跨进程共享
from apscheduler.jobstores.sqlalchemy import SQLAlchemyJobStore

self.scheduler = BackgroundScheduler(
    jobstores={
        'default': SQLAlchemyJobStore(url='mysql+pymysql://...')
    },
    timezone='Asia/Shanghai'
)
```

**优点**：
- 任务信息持久化在MySQL
- 理论上跨进程共享

**缺点**：
- APScheduler的jobstore存的是"调度信息"，与业务表`scheduler_jobs`是两套
- 需要重构现有数据模型
- 多进程同时运行会冲突（需要额外锁机制）

**结论**：**推荐方案X（轮询）**，改动小、风险低、易理解。

---

## 三、调度链三种模式详解

### 3.1 串行模式（Sequential）

```
A ──→ B ──→ C
（A完成→B开始→B完成→C开始）
```

**配置**：
```json
{
  "job_id": "chain_serial",
  "job_name": "串行调度链",
  "job_type": "chain",
  "trigger_type": "cron",
  "trigger_config": {"day_of_week": "fri", "hour": 9, "minute": 0},
  "job_config": {
    "mode": "serial",
    "steps": [
      {"job_id": "task_a", "name": "步骤A"},
      {"job_id": "task_b", "name": "步骤B"},
      {"job_id": "task_c", "name": "步骤C"}
    ],
    "continue_on_error": false
  }
}
```

**执行逻辑**：
```python
def _execute_chain_serial(self, steps, parent_id, continue_on_error):
    for step in steps:
        result = self._execute_chain_step_sync(step['job_id'], parent_id)
        if result['status'] == 'failed' and not continue_on_error:
            return f"链在 {step['job_id']} 中断"
    return "串行链完成"
```

### 3.2 并行模式（Parallel）

```
    ┌──→ A ──┐
Start├──→ B ──┤──→ (全部完成)
    └──→ C ──┘
（A、B、C同时开始，各自独立）
```

**配置**：
```json
{
  "job_id": "chain_parallel",
  "job_name": "并行调度链",
  "job_type": "chain",
  "trigger_type": "cron",
  "trigger_config": {"day_of_week": "fri", "hour": 9, "minute": 0},
  "job_config": {
    "mode": "parallel",
    "steps": [
      {"job_id": "task_a", "name": "步骤A"},
      {"job_id": "task_b", "name": "步骤B"},
      {"job_id": "task_c", "name": "步骤C"}
    ],
    "wait_all": true
  }
}
```

**执行逻辑**：
```python
def _execute_chain_parallel(self, steps, parent_id, wait_all):
    from concurrent.futures import ThreadPoolExecutor, as_completed
    
    with ThreadPoolExecutor(max_workers=len(steps)) as executor:
        futures = {
            executor.submit(self._execute_chain_step_sync, step['job_id'], parent_id): step
            for step in steps
        }
        results = []
        for future in as_completed(futures):
            results.append(future.result())
    return f"并行链完成: {len(results)}个步骤"
```

### 3.3 并行后串行模式（Parallel-then-Serial / DAG）

```
    ┌──→ A ──┐
Start├──→ B ──┤──→ D ──→ E
    └──→ C ──┘
（A、B、C并行；全部完成后→D→E串行）
```

**配置**：
```json
{
  "job_id": "chain_dag",
  "job_name": "混合调度链",
  "job_type": "chain",
  "trigger_type": "cron",
  "trigger_config": {"day_of_week": "fri", "hour": 9, "minute": 0},
  "job_config": {
    "mode": "dag",
    "stages": [
      {
        "stage": 1,
        "mode": "parallel",
        "steps": [
          {"job_id": "task_a", "name": "并行A"},
          {"job_id": "task_b", "name": "并行B"},
          {"job_id": "task_c", "name": "并行C"}
        ]
      },
      {
        "stage": 2,
        "mode": "serial",
        "steps": [
          {"job_id": "task_d", "name": "串行D"},
          {"job_id": "task_e", "name": "串行E"}
        ]
      }
    ],
    "continue_on_error": false
  }
}
```

**执行逻辑**：
```python
def _execute_chain_dag(self, stages, parent_id, continue_on_error):
    """DAG模式：按stage顺序执行，stage内按mode执行"""
    for stage in stages:
        stage_mode = stage.get('mode', 'serial')
        steps = stage.get('steps', [])
        
        if stage_mode == 'parallel':
            # 本阶段并行，等待全部完成
            results = self._execute_chain_parallel(steps, parent_id, wait_all=True)
        else:
            # 本阶段串行
            results = self._execute_chain_serial(steps, parent_id, continue_on_error)
        
        # 检查本阶段是否有失败
        if self._stage_has_failure(results) and not continue_on_error:
            return f"链在 stage{stage['stage']} 中断"
    
    return "DAG链完成"
```

### 3.4 三种模式对比

| 模式 | 结构 | 适用场景 | 配置字段 |
|------|------|----------|----------|
| **串行** | A→B→C | 有依赖，必须顺序执行 | `mode: "serial"` + `steps` |
| **并行** | A‖B‖C | 无依赖，同时执行提速 | `mode: "parallel"` + `steps` |
| **DAG** | (A‖B‖C)→D→E | 部分并行+部分依赖 | `mode: "dag"` + `stages` |

---

## 四、完整参数配置详解

### 4.1 触发器参数（trigger_config）

#### Cron触发器（定时）

```json
{
  "trigger_type": "cron",
  "trigger_config": {
    "year": "*",           // 年（可选）
    "month": "*",          // 月 1-12
    "day": "*",            // 日 1-31
    "week": "*",           // 周数 1-53
    "day_of_week": "fri",  // 星期几 mon-sun 或 0-6
    "hour": 9,             // 时 0-23
    "minute": 0,           // 分 0-59
    "second": 0            // 秒 0-59
  }
}
```

**常用示例**：
```json
// 每周五09:00
{"day_of_week": "fri", "hour": 9, "minute": 0}

// 每天09:30
{"hour": 9, "minute": 30}

// 每月1号00:00
{"day": 1, "hour": 0, "minute": 0}

// 工作日每天15:00
{"day_of_week": "mon-fri", "hour": 15, "minute": 0}

// 每小时执行
{"minute": 0}
```

#### Interval触发器（间隔）

```json
{
  "trigger_type": "interval",
  "trigger_config": {
    "weeks": 0,
    "days": 0,
    "hours": 1,      // 每1小时
    "minutes": 0,
    "seconds": 0
  }
}
```

#### Date触发器（一次性）

```json
{
  "trigger_type": "date",
  "trigger_config": {
    "run_date": "2026-08-01 09:00:00"
  }
}
```

### 4.2 任务参数（job_config）

#### Python脚本任务

```json
{
  "job_type": "python_script",
  "job_config": {
    "script_path": "gs2026.analysis.worker.xxx",  // 模块路径
    "function": "main",                            // 函数名
    "params": {                                    // 参数
      "base_date": "2026-07-29",
      "mode": "full"
    },
    "working_dir": "F:/pyworkspace2026/gs2026",
    "timeout": 3600                                // 超时秒数
  }
}
```

#### 函数任务

```json
{
  "job_type": "function",
  "job_config": {
    "module": "gs2026.xxx.yyy",
    "function": "process_data",
    "params": {"key": "value"}
  }
}
```

### 4.3 调度链参数（完整）

```json
{
  "job_id": "full_pipeline",
  "job_name": "完整数据流水线",
  "job_type": "chain",
  "trigger_type": "cron",
  "trigger_config": {"day_of_week": "fri", "hour": 9, "minute": 0},
  "job_config": {
    "mode": "dag",                    // serial/parallel/dag
    "continue_on_error": false,       // 错误时是否继续
    "timeout": 7200,                  // 整链超时
    "stages": [                       // DAG模式用stages
      {
        "stage": 1,
        "mode": "parallel",
        "steps": [
          {
            "job_id": "collect_stock",
            "name": "采集股票",
            "timeout": 1800,
            "retry": 2                 // 重试次数
          },
          {
            "job_id": "collect_bond",
            "name": "采集债券",
            "timeout": 1800
          }
        ]
      },
      {
        "stage": 2,
        "mode": "serial",
        "steps": [
          {"job_id": "process_data", "name": "处理数据"},
          {"job_id": "gen_report", "name": "生成报告"}
        ]
      }
    ],
    "notification": {                 // 通知配置
      "on_complete": true,
      "on_error": true,
      "channels": ["log", "message"]
    }
  },
  "status": "enabled"
}
```

---

## 五、结合当前代码的完整改造清单

### 5.1 需要修改的文件

| 文件 | 改动 | 目的 |
|------|------|------|
| `scheduler_service.py` | 移除app.py进程的add调度，增强链式执行，加轮询同步 | 核心逻辑 |
| `scheduler.py` | 移除start/stop，改为查询服务状态 | 路由层 |
| `scheduler_daemon.py`（新建）| Windows服务包装 | 独立进程 |
| `scheduler.html` | 链式配置界面（串行/并行/DAG）| 前端 |
| `scheduler.js` | 服务状态显示，链式配置逻辑 | 前端 |
| `create_scheduler_tables.sql` | 新增变更信号表 | 数据库 |
| 安装脚本（新建）| 服务安装/管理 | 运维 |

### 5.2 核心代码改造

#### 改造1：scheduler_service.py 区分运行模式

```python
class SchedulerService:
    def __init__(self, mode='app'):
        """
        mode: 
          - 'app': app.py内运行（只写MySQL，不调度）
          - 'daemon': Windows服务运行（唯一调度者）
        """
        self.mode = mode
        # ...
    
    def add_job(self, job_config: Dict) -> str:
        # 写入MySQL（两种模式都写）
        execute_update(sql, (...))
        
        # 只有daemon模式才add到调度器
        if self.mode == 'daemon' and job_config.get('status') == 'enabled':
            self._add_job_to_scheduler(job[0])
        
        # app模式：写变更信号，让daemon轮询感知
        if self.mode == 'app':
            self._write_reload_signal(job_id, 'add')
        
        return job_id
```

#### 改造2：新增变更信号表

```sql
CREATE TABLE scheduler_reload_signal (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    job_id VARCHAR(64),
    action VARCHAR(16),      -- add/update/delete/toggle
    processed TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_processed (processed)
);
```

#### 改造3：daemon轮询处理信号

```python
def _sync_jobs_from_db(self):
    """轮询处理变更信号（daemon模式）"""
    signals = execute_query(
        "SELECT * FROM scheduler_reload_signal WHERE processed=0 ORDER BY id"
    )
    for sig in signals:
        job_id = sig['job_id']
        action = sig['action']
        
        if action in ('add', 'update', 'toggle'):
            self._reload_single_job(job_id)
        elif action == 'delete':
            try:
                self.scheduler.remove_job(job_id)
            except:
                pass
        
        # 标记已处理
        execute_update(
            "UPDATE scheduler_reload_signal SET processed=1 WHERE id=%s",
            (sig['id'],)
        )
```

---

## 六、完整数据流（改造后）

```
【配置阶段】
用户前端设置调度链 A→B→C
    │
    ▼ POST /api/scheduler/jobs
app.py: 写MySQL(scheduler_jobs) + 写信号(scheduler_reload_signal)
    │
    ▼ 30秒内
Windows服务: 轮询发现信号 → 加载任务到APScheduler
    │
【执行阶段】（app.py可以关闭）
周五09:00 触发器触发
    │
    ▼
Windows服务执行调度链:
    Stage1并行: A‖B‖C（同时）
    ↓ 全部完成
    Stage2串行: D→E（顺序）
    │
    ▼
写执行记录(scheduler_execution_log)
    │
【查看阶段】（需要app.py）
用户打开app.py → 前端查看执行记录/状态
```

---

## 七、关键问题最终解答

### Q1: 前端设置后，读MySQL的是Windows独立服务还是app.py？

**改造后**：
- 前端设置 → app.py**只写MySQL**（不调度）
- **Windows独立服务**轮询MySQL → 加载任务 → 调度执行
- **调度的唯一执行者是Windows服务**，app.py只是配置入口

### Q2: app.py不启动能调度吗？

**能**。配置一旦写入MySQL，Windows服务就会加载执行，与app.py无关。

### Q3: 当前代码为什么不行？

因为当前 `add_job` 把任务加到**app.py进程的内存调度器**，app.py关闭则调度器销毁。且APScheduler用MemoryJobStore，无持久化。

### Q4: 三种调度链模式如何选？

- **串行**：任务有严格依赖（A的输出是B的输入）
- **并行**：任务无依赖，追求速度
- **DAG**：混合场景（先并行采集，再串行处理）

---

## 八、实施步骤（待确认后执行）

- [ ] 阶段1：改造scheduler_service.py（区分app/daemon模式）
- [ ] 阶段2：新增变更信号表 + 轮询同步
- [ ] 阶段3：创建scheduler_daemon.py（Windows服务）
- [ ] 阶段4：增强调度链（串行/并行/DAG三模式）
- [ ] 阶段5：前端界面改造（链式配置 + 服务状态）
- [ ] 阶段6：安装脚本 + 集成测试

---

**本文档持续更新，后续版本追加在下方。**

<!-- 
# ========================================
# v4.0（预留）
# ========================================
后续更新写在这里
-->

---

# ========================================
# v4.0（当前版本）2026-07-29
# ========================================

## 一、【问题1】交易日动态调度（基于MySQL查询控制）

### 1.1 你的需求

> "我只想交易日执行，交易日可能是周三、周四，下周可能是周二、周五。能否通过SQL确定交易时间再转换成调度时间？"

**答案**：✅ **完全可以**，而且你的系统已经有现成的交易日历表 `data_jyrl`。

### 1.2 现有基础（已确认）

**表 `data_jyrl`**（交易日历表）：
```
trade_date    DATE    日期
trade_status  INT     1=交易日, 0=休市
```

**实测数据**（已验证）：
```
2026-07-29 - 交易日
2026-07-30 - 交易日
2026-07-31 - 交易日
2026-08-01 - 休市（周六）
2026-08-02 - 休市（周日）
2026-08-03 - 交易日
...
```

**现成工具** `trading_day_util.py`：
```python
get_prev_trading_day(ref_date)   # 上一交易日
get_next_trading_day(ref_date)   # 下一交易日
get_date_list(ref_date)          # 日期区间
```

### 1.3 核心难点分析

**Cron的局限**：
- 标准cron只能表达"固定规律"（如每周五、每天9点）
- **无法表达"交易日"**（交易日不规律：遇节假日会变）

**举例**：
```
国庆节 2026-10-01~10-07 休市
- 9-30(周三)是交易日
- 10-08(周四)才是下一个交易日
- 中间的10-01~10-07全部跳过
标准cron做不到这种"动态跳过"
```

### 1.4 解决方案：两种模式

#### 方案A：每日检查 + 交易日判断（推荐，简单可靠）

**原理**：每天固定时间触发，但执行前先查 `data_jyrl` 判断今天是否交易日，非交易日直接跳过。

```python
# 新增触发器类型: 'trading_cron'（交易日专用）
def _create_trigger(self, trigger_type, trigger_config):
    if trigger_type == 'trading_cron':
        # 底层仍用cron每天触发，但包装交易日判断
        return CronTrigger(
            hour=trigger_config.get('hour', 9),
            minute=trigger_config.get('minute', 30)
        )
    # ...其他类型

# 任务执行前判断交易日
def _execute_job_by_id(self, job_id, execution_id):
    job = self.get_job(job_id)
    trigger_config = json.loads(job['trigger_config'])
    
    # 如果是交易日专用触发器，先判断今天是否交易日
    if job['trigger_type'] == 'trading_cron':
        if not self._is_trading_day_today():
            logger.info(f"今日非交易日，跳过任务: {job_id}")
            self._finish_execution(execution_id, 'skipped', '非交易日跳过')
            return
    
    # 是交易日，正常执行
    # ...

def _is_trading_day_today(self) -> bool:
    """查询data_jyrl判断今天是否交易日"""
    sql = "SELECT trade_status FROM data_jyrl WHERE trade_date = CURDATE()"
    result = execute_query(sql)
    return result and result[0]['trade_status'] == 1
```

**配置示例**：
```json
{
  "job_id": "trading_task",
  "job_name": "交易日数据处理",
  "trigger_type": "trading_cron",
  "trigger_config": {
    "hour": 9,
    "minute": 30,
    "check_field": "trade_status",
    "check_table": "data_jyrl"
  }
}
```

**优点**：
- 逻辑简单，用现成的cron机制
- 交易日历更新（如临时调休），自动适应
- 执行记录会记录"skipped"状态，可追溯

**缺点**：
- 非交易日也会"触发"（但立即跳过，开销极小）

#### 方案B：动态计算下次执行时间（高级，精确）

**原理**：每次执行后，查询 `data_jyrl` 计算下一个交易日，动态设置下次触发时间。

```python
def _schedule_next_trading_day(self, job_id, hour, minute):
    """动态调度到下一个交易日"""
    # 查询下一个交易日
    sql = """
        SELECT trade_date FROM data_jyrl 
        WHERE trade_date > CURDATE() AND trade_status = 1 
        ORDER BY trade_date ASC LIMIT 1
    """
    result = execute_query(sql)
    if not result:
        logger.warning("未找到下一个交易日")
        return
    
    next_trading_date = result[0]['trade_date']
    run_date = datetime.combine(next_trading_date, time(hour, minute))
    
    # 动态添加date触发器
    self.scheduler.add_job(
        func=self._execute_and_reschedule,
        trigger='date',
        run_date=run_date,
        args=[job_id, hour, minute],
        id=f"{job_id}_next",
        replace_existing=True
    )
    logger.info(f"下次执行时间: {run_date}")

def _execute_and_reschedule(self, job_id, hour, minute):
    """执行任务并调度下一个交易日"""
    self._execute_job_by_id(job_id, self._create_execution(job_id, 'trading'))
    # 执行完后，立即调度下一个交易日
    self._schedule_next_trading_day(job_id, hour, minute)
```

**优点**：
- 精确，只在交易日触发
- 无空触发

**缺点**：
- 逻辑复杂，依赖"执行后重新调度"链条
- 若服务重启，需要重新初始化下次调度
- 交易日历临时变更时，已调度的可能不准

### 1.5 推荐选择

| 场景 | 推荐方案 |
|------|----------|
| **一般交易日任务** | **方案A（每日检查+跳过）** |
| 对精确性要求极高 | 方案B（动态计算）|

**理由**：方案A简单可靠，"非交易日空触发+立即跳过"的开销可忽略（每天1次判断），且对交易日历变更天然适应。

### 1.6 SQL查询交易时间转调度的完整逻辑

```python
def get_trading_schedule_info(self, days_ahead=30):
    """查询未来交易日，供前端展示调度预览"""
    sql = """
        SELECT trade_date FROM data_jyrl 
        WHERE trade_date >= CURDATE() 
          AND trade_date <= DATE_ADD(CURDATE(), INTERVAL %s DAY)
          AND trade_status = 1 
        ORDER BY trade_date
    """
    trading_days = execute_query(sql, (days_ahead,))
    return [str(d['trade_date']) for d in trading_days]

# 前端展示: "接下来的交易日: 07-29, 07-30, 07-31, 08-03, 08-04..."
# 用户就能看到任务实际会在哪些天执行
```

---

## 二、【问题2】前端执行状态可视化

### 2.1 你的需求

> "前端如何判断调度是否执行？执行成功还是失败？执行到哪个阶段了？"

### 2.2 现有基础（已确认）

**表 `scheduler_execution_log`**（执行记录，已有完整字段）：
```
id                    bigint
job_id                varchar(64)     -- 任务ID
execution_id          varchar(64)     -- 执行ID（唯一）
trigger_type          enum('scheduled','manual','chain')  -- 触发方式
parent_execution_id   varchar(64)     -- 父执行ID（链式关联）★
start_time            datetime        -- 开始时间
end_time              datetime        -- 结束时间
duration_seconds      int             -- 耗时
status                enum('pending','running','success','failed','timeout','skipped')  ★
result_message        text            -- 结果消息
error_type            varchar(64)     -- 错误类型
error_stack           text            -- 错误堆栈
output_log            text            -- 输出日志
next_executions       json            -- 后续执行
created_at            timestamp
```

**关键字段**：
- `status`：6种状态，覆盖所有情况 ✅
- `parent_execution_id`：链式任务的父子关联 ✅
- `start_time`/`end_time`/`duration_seconds`：时间追踪 ✅

### 2.3 三个问题的解答

#### Q2.1：如何判断调度是否执行？

**看 `scheduler_execution_log` 是否有记录**：
```sql
-- 查询某任务今天是否执行过
SELECT * FROM scheduler_execution_log 
WHERE job_id = 'task_a' 
  AND DATE(start_time) = CURDATE()
ORDER BY start_time DESC;

-- 有记录 = 已执行/执行中
-- 无记录 = 未执行（可能未到时间，或非交易日跳过）
```

#### Q2.2：执行成功还是失败？

**看 `status` 字段**：
```
pending   - 等待中（已创建，未开始）
running   - 执行中
success   - 成功 ✅
failed    - 失败 ❌
timeout   - 超时 ⏱️
skipped   - 跳过（如非交易日）⏭️
```

#### Q2.3：执行到哪个阶段了？（调度链进度）

**通过 `parent_execution_id` 关联查询链的所有步骤**：
```sql
-- 查询某调度链的所有步骤状态
SELECT 
    job_id,
    execution_id,
    status,
    start_time,
    end_time,
    duration_seconds
FROM scheduler_execution_log 
WHERE parent_execution_id = 'chain_xxx_execution_id'
   OR execution_id = 'chain_xxx_execution_id'
ORDER BY start_time;

-- 结果就是链的执行进度:
-- chain_xxx    | running | ...        （链本身在运行）
--   task_a     | success | 09:00-09:05 （步骤A完成）
--   task_b     | running | 09:05-...   （步骤B执行中）★当前阶段
--   task_c     | pending | ...         （步骤C等待）
```

### 2.4 前端可视化设计

#### 界面1：任务列表（总览）

```
┌────────────────────────────────────────────────────────────┐
│ 任务名称    │ 类型  │ 最近执行  │ 状态    │ 下次执行         │
├────────────────────────────────────────────────────────────┤
│ 交易日采集  │ 脚本  │ 09:30    │ ✅成功  │ 07-30 09:30     │
│ 每周流水线  │ 调度链│ 09:00    │ 🔄运行中│ 08-01 09:00     │
│ 数据备份    │ 脚本  │ 昨天     │ ❌失败  │ 今天 23:00      │
└────────────────────────────────────────────────────────────┘
```

#### 界面2：调度链进度（详情）

```
┌────────────────────────────────────────────────────────────┐
│ 调度链: 每周数据流水线                    状态: 🔄 运行中     │
│ 开始: 2026-07-29 09:00:00                                    │
├────────────────────────────────────────────────────────────┤
│  Stage 1 (并行)                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                    │
│  │✅ 采集股票│ │✅ 采集债券│ │✅ 采集行业│  全部完成 5分12秒 │
│  └──────────┘ └──────────┘ └──────────┘                    │
│         │                                                    │
│         ▼                                                    │
│  Stage 2 (串行)                                              │
│  ┌──────────┐                                               │
│  │🔄 处理数据│ ← 当前阶段（运行中 2分30秒）                 │
│  └──────────┘                                               │
│         │                                                    │
│         ▼                                                    │
│  ┌──────────┐                                               │
│  │⏸️ 生成报告│ 等待中                                        │
│  └──────────┘                                               │
└────────────────────────────────────────────────────────────┘
```

#### 界面3：实时状态徽章

```javascript
// 状态徽章样式
const statusBadge = {
    'pending':  { icon: '⏸️', text: '等待中', color: '#909399' },
    'running':  { icon: '🔄', text: '运行中', color: '#409EFF', animate: true },
    'success':  { icon: '✅', text: '成功',   color: '#67C23A' },
    'failed':   { icon: '❌', text: '失败',   color: '#F56C6C' },
    'timeout':  { icon: '⏱️', text: '超时',   color: '#E6A23C' },
    'skipped':  { icon: '⏭️', text: '跳过',   color: '#C0C4CC' }
};
```

### 2.5 前端API设计

#### API1：获取任务执行状态

```python
@scheduler_bp.route('/jobs/<job_id>/latest-status', methods=['GET'])
def get_job_latest_status(job_id):
    """获取任务最近执行状态"""
    sql = """
        SELECT status, start_time, end_time, duration_seconds, result_message
        FROM scheduler_execution_log 
        WHERE job_id = %s 
        ORDER BY start_time DESC LIMIT 1
    """
    result = execute_query(sql, (job_id,))
    return jsonify({'code': 200, 'data': result[0] if result else None})
```

#### API2：获取调度链进度

```python
@scheduler_bp.route('/chains/<execution_id>/progress', methods=['GET'])
def get_chain_progress(execution_id):
    """获取调度链执行进度"""
    sql = """
        SELECT job_id, execution_id, status, start_time, end_time, 
               duration_seconds, parent_execution_id
        FROM scheduler_execution_log 
        WHERE execution_id = %s OR parent_execution_id = %s
        ORDER BY start_time
    """
    steps = execute_query(sql, (execution_id, execution_id))
    
    # 计算整体进度
    total = len(steps) - 1  # 减去链本身
    completed = sum(1 for s in steps if s['status'] == 'success')
    
    return jsonify({
        'code': 200,
        'data': {
            'steps': steps,
            'progress': f"{completed}/{total}",
            'current_stage': next((s['job_id'] for s in steps if s['status'] == 'running'), None)
        }
    })
```

#### API3：前端轮询刷新

```javascript
// 前端每3秒轮询运行中的链
async function pollChainProgress(executionId) {
    const resp = await fetch(`/api/scheduler/chains/${executionId}/progress`);
    const data = await resp.json();
    
    // 更新进度条
    updateProgressUI(data.data.steps);
    
    // 如果还在运行，继续轮询
    const isRunning = data.data.steps.some(s => s.status === 'running');
    if (isRunning) {
        setTimeout(() => pollChainProgress(executionId), 3000);
    }
}
```

### 2.6 关键增强：链式执行时写入每步记录

**当前问题**：现有 `_execute_chain_job` 可能没有为每个步骤创建独立记录。

**改造**：确保链的每一步都写 `scheduler_execution_log`，用 `parent_execution_id` 关联：

```python
def _execute_chain_step_sync(self, job_id, parent_execution_id):
    """执行链中一步，写入独立执行记录"""
    # 为这一步创建执行记录（关联父链）
    step_execution_id = self._create_execution(
        job_id, 'chain', parent_execution_id  # ← parent关联
    )
    
    try:
        self._update_execution_status(step_execution_id, 'running')  # 标记运行中
        result = self._execute_job_by_id(job_id, step_execution_id)
        self._finish_execution(step_execution_id, 'success', result)
        return {'status': 'success', 'execution_id': step_execution_id}
    except Exception as e:
        self._finish_execution(step_execution_id, 'failed', str(e))
        return {'status': 'failed', 'execution_id': step_execution_id}
```

这样前端就能通过 `parent_execution_id` 查到链的完整进度。

---

## 三、v4.0 实施清单（增量）

### 交易日调度（问题1）

- [ ] 新增触发器类型 `trading_cron`
- [ ] 新增 `_is_trading_day_today()` 查询data_jyrl
- [ ] 任务执行前判断交易日，非交易日标记 `skipped`
- [ ] 新增 `get_trading_schedule_info()` 供前端预览交易日
- [ ] 前端展示"接下来的交易日执行预览"

### 执行状态可视化（问题2）

- [ ] 新增 API `/jobs/<job_id>/latest-status`
- [ ] 新增 API `/chains/<execution_id>/progress`
- [ ] 增强 `_execute_chain_step_sync` 写每步记录
- [ ] 前端任务列表状态徽章
- [ ] 前端调度链进度可视化（Stage + Step）
- [ ] 前端轮询刷新运行中的链

---

## 四、v4.0 关键问题解答

### Q1: 交易日不规律（周三周四/周二周五），能调度吗？

**能**。方案A：每天固定时间触发，执行前查 `data_jyrl`，非交易日跳过。交易日历表已有未来数据，临时调休也自动适应。

### Q2: 如何知道任务执行了没有？

查 `scheduler_execution_log` 有无当天记录。有=执行过，无=未执行。

### Q3: 成功失败怎么看？

看 `status` 字段：success/failed/timeout/skipped/running/pending 六种状态。

### Q4: 调度链执行到哪一步了？

通过 `parent_execution_id` 查询链的所有步骤，找 `status='running'` 的就是当前阶段。前端可视化展示Stage和Step进度。

---

**本文档持续更新，后续版本追加在下方。**

<!-- 
# ========================================
# v5.0（预留）
# ========================================
后续更新写在这里
-->
