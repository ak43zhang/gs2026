# 多级缓存与中间状态统一存储方案

**文档版本**: v7.0（最终版）  
**生成时间**: 2026-07-26 05:17  
**状态**: 🟡 待审核

---

## 版本沿革

| 版本 | 时间 | 变更内容 | 状态 |
|------|------|----------|------|
| v1.0 | 2026-07-26 03:51 | 初始方案：新建 `ext_state` 字段存储中间状态 | 废弃 |
| v2.0 | 2026-07-26 04:03 | 优化：中间字段放入 `ext_indicators`，`_state_` 前缀 | 废弃 |
| v3.0 | 2026-07-26 04:20 | 最终方案：JSON同构，`_m_`前缀，个券分级存储 | 废弃 |
| v4.0 | 2026-07-26 04:41 | 优化：解决膨胀问题，`_m_shape_h`压缩存储 | 废弃 |
| v5.0 | 2026-07-26 04:51 | 独立存储：`_m_shape_h`独立存Redis/MySQL，ext存固定大小 | 废弃 |
| **v6.0** | **2026-07-26 05:10** | **通用单表：`day_history_intermediate`单表支持多指标** | **废弃** |
| **v7.0** | **2026-07-26 05:17** | **最终版：整合v5+v6，清晰流程，通用中间过程缓存架构** | **待审核** |

---

## 一、整体设计目标

### 1.1 核心问题

| 问题 | 说明 |
|------|------|
| 中间字段膨胀 | `_mkt_shape_history`等字段随时间不断增长 |
| 重复存储 | 每个tick的ext_indicators都包含全量历史 |
| 重启丢失 | 服务重启后累积状态无法恢复 |
| 回填不一致 | 实时和回填使用不同恢复逻辑 |

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| **分离存储** | 累积型历史独立存储，ext_indicators只存固定大小结果 |
| **通用架构** | 单表支持多指标，通过`indicator`字段区分 |
| **四级缓存** | L1内存→L2Redis→L3MySQL→L4重新计算 |
| **实时回填一致** | 同一套恢复逻辑，同一套存储架构 |
| **零侵入** | 不影响现有计算逻辑，失败自动降级 |

---

## 二、架构概览

### 2.1 存储分离架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        存储分离架构                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────┐        ┌─────────────────────────────┐ │
│  │  ext_indicators     │        │  IntermediateCache          │ │
│  │  （每tick存储）      │        │  （累积型历史独立存储）       │ │
│  ├─────────────────────┤        ├─────────────────────────────┤ │
│  │  结果字段：          │        │  支持指标：                  │ │
│  │  - mkt_vw_bias      │        │  - mkt_shape（大盘形态）      │ │
│  │  - mkt_shp          │        │  - bond_slope_{code}         │ │
│  │  - ...              │        │  - mkt_vwap                  │ │
│  │                     │        │  - ...（可扩展）              │ │
│  │  固定大小中间状态：  │        │                             │ │
│  │  - _m_vwap_pv       │        │  四级缓存：                  │ │
│  │  - _m_high/low      │        │  - L1: 内存                  │ │
│  │  （无大list）        │        │  - L2: Redis（12h过期）       │ │
│  │                     │        │  - L3: MySQL单表              │ │
│  │                     │        │  - L4: 重新计算               │ │
│  └─────────────────────┘        └─────────────────────────────┘ │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 四级缓存流程

```
实时运行（每tick）：
  ├─ 1. 计算当前指标值
  ├─ 2. IntermediateCache.append(indicator, date, time, value)
  │   ├─ L1: 内存追加（<1μs）
  │   ├─ L2: 标记待写入Redis（异步批量）
  │   └─ L3: 标记待写入MySQL（异步批量）
  ├─ 3. 从L1内存获取历史，计算结果
  └─ 4. 存储ext_indicators（固定大小，无大list）

重启恢复：
  ├─ 1. IntermediateCache.get_range(indicator, date, start, end)
  │   ├─ L1: 内存？无（刚重启）
  │   ├─ L2: Redis HGETALL → 内存筛选区间
  │   ├─ L3: MySQL SELECT ... WHERE time_sec BETWEEN
  │   └─ L4: 返回空list（从0开始）
  └─ 2. 恢复正常运行

回填（data_recovery.py）：
  ├─ 1. 从L2/L3加载历史到L1内存
  ├─ 2. 计算当前tick
  ├─ 3. IntermediateCache.append（更新L1/L2/L3）
  └─ 4. 存储结果到monitor_zq_sssj
```

---

## 三、数据存储设计

### 3.1 MySQL：通用单表

```sql
-- 表名: day_history_intermediate
-- 用途: 通用中间过程缓存，单表支持所有指标

CREATE TABLE day_history_intermediate (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    indicator VARCHAR(64) NOT NULL COMMENT '指标名，如mkt_shape、bond_slope_123456',
    trading_date VARCHAR(8) NOT NULL COMMENT '交易日YYYYMMDD',
    time_sec INT NOT NULL COMMENT '时间（秒，从0点开始）',
    value_json JSON NOT NULL COMMENT '该时间点的值',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    -- 核心索引：支持区间查询
    UNIQUE KEY uk_indicator_date_time (indicator, trading_date, time_sec),
    KEY idx_indicator_date (indicator, trading_date),
    KEY idx_date (trading_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='中间过程缓存表（通用）';

-- 查询示例：获取mkt_shape全天历史形成list
SELECT time_sec, value_json 
FROM day_history_intermediate 
WHERE indicator = 'mkt_shape' 
  AND trading_date = '20260726'
  AND time_sec BETWEEN 32400 AND 54000
ORDER BY time_sec;
```

### 3.2 Redis：通用Hash结构

```python
# Key: day_hist:{indicator}:{date}
# Type: Hash
# Field: time_sec
# Value: value_json
# TTL: 43200秒（12小时）

# 示例：
# Key: day_hist:mkt_shape:20260726
#   field: "32400" → value: '{"p": -0.5}'
#   field: "32403" → value: '{"p": -0.48}'
#   field: "32406" → value: '{"p": -0.47}'
#   ...
# TTL: 43200

# 操作：
# HSET day_hist:mkt_shape:20260726 32400 '{"p": -0.5}'
# HGETALL day_hist:mkt_shape:20260726  → 获取全天
# HSET + EXPIRE 43200
```

### 3.3 ext_indicators：固定大小

```python
# 每tick存储到ext_indicators（固定大小，不膨胀）
ext_indicators = {
    # ====== 结果字段（原有） ======
    "mkt_vw_bias": -0.3,
    "mkt_shp": "低开高走",
    "mkt_shd": "低点在开盘后15分钟",
    # ...
    
    # ====== 固定大小中间状态（标量或小缓存） ======
    "_m_vwap_pv": 12345678.9,
    "_m_vwap_v": 987654321.0,
    "_m_high": 1.52,
    "_m_low": -0.85,
    # ...
    
    # 注意：无大list！形态历史等累积型字段独立存储
}
```

---

## 四、核心模块：IntermediateCache

### 4.1 类设计

```python
class IntermediateCache:
    """
    通用中间过程缓存模块
    
    职责：
    1. 累积型历史的四级缓存管理
    2. 区间查询形成list
    3. 实时/回填统一接口
    """
    
    # L1: 内存缓存 {(indicator, date): [(time_sec, value), ...]}
    _memory_cache = {}
    
    @staticmethod
    def append(indicator: str, date: str, time_sec: int, value: dict):
        """
        追加单点（每tick调用）
        
        流程：
        1. L1内存追加
        2. 标记异步写入L2/L3
        """
        key = (indicator, date)
        
        # L1: 内存追加
        if key not in IntermediateCache._memory_cache:
            IntermediateCache._memory_cache[key] = []
        IntermediateCache._memory_cache[key].append((time_sec, value))
        
        # 异步批量写入L2/L3（每30秒或每100点）
        IntermediateCache._async_persist(indicator, date, time_sec, value)
    
    @staticmethod
    def get_range(indicator: str, date: str, 
                  start_time: int = None, 
                  end_time: int = None) -> list:
        """
        获取区间数据形成list（四级缓存）
        
        返回: [{'t': time_sec, **value}, ...] 按时间排序
        """
        key = (indicator, date)
        start_time = start_time or 32400  # 默认09:30:00
        end_time = end_time or 54000       # 默认15:00:00
        
        # L1: 内存查询
        if key in IntermediateCache._memory_cache:
            result = [{'t': t, **v} for t, v in IntermediateCache._memory_cache[key]
                     if start_time <= t <= end_time]
            if result:
                return sorted(result, key=lambda x: x['t'])
        
        # L2: Redis查询
        result = IntermediateCache._get_from_redis(indicator, date, start_time, end_time)
        if result:
            IntermediateCache._fill_memory(key, result)
            return result
        
        # L3: MySQL查询
        result = IntermediateCache._get_from_mysql(indicator, date, start_time, end_time)
        if result:
            IntermediateCache._fill_memory(key, result)
            IntermediateCache._fill_redis(indicator, date, result)
            return result
        
        # L4: 无数据
        return []
```

### 4.2 四级缓存实现

```python
    @staticmethod
    def _get_from_redis(indicator: str, date: str,
                        start_time: int, end_time: int) -> Optional[list]:
        """L2: Redis HGETALL获取全天，内存筛选区间"""
        try:
            r = redis_util._get_redis_client()
            key = f"day_hist:{indicator}:{date}"
            data = r.hgetall(key)
            if not data:
                return None
            
            result = []
            for t_bytes, v_bytes in data.items():
                t = int(t_bytes.decode())
                if start_time <= t <= end_time:
                    result.append({'t': t, **json.loads(v_bytes.decode())})
            
            return sorted(result, key=lambda x: x['t'])
        except Exception as e:
            logger.warning(f"[L2] Redis失败: {e}")
            return None
    
    @staticmethod
    def _get_from_mysql(indicator: str, date: str,
                        start_time: int, end_time: int) -> Optional[list]:
        """L3: MySQL区间查询"""
        try:
            sql = """
                SELECT time_sec, value_json 
                FROM day_history_intermediate
                WHERE indicator = %(indicator)s
                  AND trading_date = %(date)s
                  AND time_sec BETWEEN %(start)s AND %(end)s
                ORDER BY time_sec
            """
            df = pd.read_sql(sql, engine, params={
                'indicator': indicator, 'date': date,
                'start': start_time, 'end': end_time
            })
            
            if df.empty:
                return None
            
            return [{'t': row['time_sec'], **json.loads(row['value_json'])}
                    for _, row in df.iterrows()]
        except Exception as e:
            logger.warning(f"[L3] MySQL失败: {e}")
            return None
```

---

## 五、应用到 `_mkt_shape_history`

### 5.1 改造前（问题）

```python
# 每tick：ext_indicators包含全量历史（膨胀！）
ext_indicators = {
    "mkt_vw_bias": -0.3,
    "_m_shape_h": [
        {"t": 32400, "p": -0.5},  # 09:30:00
        {"t": 32403, "p": -0.48}, # 09:30:03
        # ... 4800个点，不断膨胀
    ],
}
# 问题：每个tick都存4800个点，重复存储4800次！
```

### 5.2 改造后（v7.0）

```python
# monitor_bond.py 改造

def compute_mkt_trend_indicators_v7(df_now, time_full, current_date):
    """v7.0: 使用IntermediateCache"""
    global _mkt_shape_date, _mkt_shape_history
    
    current_seconds = _time_to_seconds(time_full)
    mkt_vs_open_pct = round(float(df_now['change_pct'].mean()), 4)
    
    # 日期切换 → 从四级缓存恢复
    if _mkt_shape_date != current_date:
        _mkt_shape_history = IntermediateCache.get_range(
            indicator='mkt_shape',
            date=current_date,
            start_time=32400,
            end_time=current_seconds
        )
        _mkt_shape_date = current_date
        logger.info(f"[形态恢复] 加载 {len(_mkt_shape_history)} 点")
    
    # 追加到四级缓存
    IntermediateCache.append(
        indicator='mkt_shape',
        date=current_date,
        time_sec=current_seconds,
        value={'p': mkt_vs_open_pct}
    )
    
    # 更新内存list
    _mkt_shape_history.append({'t': current_seconds, 'p': mkt_vs_open_pct})
    
    # 计算形态
    history_for_calc = _mkt_shape_history[:-1] if len(_mkt_shape_history) > 1 else []
    mkt_shape = compute_mkt_shape({'mkt_vs_open_pct': mkt_vs_open_pct}, history_for_calc)
    
    # 返回结果（ext_indicators不再包含大list）
    return {
        'mkt_vw_bias': mkt_vwap_bias,
        'mkt_shp': mkt_shape,
        # 无 _m_shape_h！
    }
```

### 5.3 ext_indicators 变化

```python
# v7.0: ext_indicators 固定大小，不膨胀
ext_indicators = {
    # 结果字段
    "mkt_vw_bias": -0.3,
    "mkt_shp": "低开高走",
    
    # 固定大小中间状态
    "_m_vwap_pv": 12345678.9,
    "_m_high": 1.52,
    # ...
    
    # 形态历史独立存储：
    # - L1: _mkt_shape_history (内存)
    # - L2: day_hist:mkt_shape:20260726 (Redis, 12h)
    # - L3: day_history_intermediate (MySQL)
}
```

---

## 六、扩展到其他指标

### 6.1 配置化（无需改表）

```python
# 指标注册表（仅文档，不影响存储）
REGISTERED_INDICATORS = {
    'mkt_shape': {
        'description': '大盘形态历史',
        'value_schema': {'p': float},
    },
    'bond_slope': {
        'description': '个券斜率历史',
        'value_schema': {'ss': float, 'sl': float},
    },
    # 新增指标只需在这里注册，无需改表结构
}

# 动态指标名
def make_indicator(base: str, code: str = None) -> str:
    return f"{base}_{code}" if code else base

# 使用示例
indicator = make_indicator('bond_slope', '123456')
IntermediateCache.append(indicator=indicator, ...)
```

### 6.2 个券斜率示例

```python
# 400只债券，每只独立indicator
for code in bond_codes:
    indicator = f"bond_slope_{code}"
    
    # 追加
    IntermediateCache.append(
        indicator=indicator,
        date=current_date,
        time_sec=current_seconds,
        value={'ss': slope_short, 'sl': slope_long}
    )
    
    # 查询历史
    history = IntermediateCache.get_range(
        indicator=indicator,
        date=current_date,
        start_time=32400,
        end_time=current_seconds
    )
```

---

## 七、性能与存储分析

### 7.1 存储对比

| 方案 | ext_indicators | Redis | MySQL | 总存储 |
|------|----------------|-------|-------|--------|
| v3.0（原） | 720MB/天（重复4800次） | - | - | **720MB** |
| v7.0（新） | 固定（无大list） | 76KB×1次 | 76KB×1次 | **~76KB** |
| **优化** | **-99.99%** | 新增 | 新增 | **-99%** |

### 7.2 查询性能

| 场景 | 耗时 | 说明 |
|------|------|------|
| L1内存 | <1μs | 正常运行 |
| L2Redis | 2-5ms | 重启后首次 |
| L3MySQL | 10-50ms | Redis未命中 |
| L4重新计算 | 100ms+ | 降级 |

### 7.3 可靠性

| 场景 | 处理 |
|------|------|
| Redis宕机 | 从MySQL恢复，降级可用 |
| MySQL缺失 | 从Redis恢复，或从0开始 |
| 都缺失 | 从0开始，10-15分钟恢复 |

---

## 八、实施计划

### 阶段1：基础模块（1天）

1. **新建 `intermediate_cache.py`**
   - IntermediateCache 类
   - 四级缓存实现
   - 异步批量写入

2. **MySQL表创建**
   - `day_history_intermediate` 单表

### 阶段2：改造形态历史（1天）

1. **改造 `compute_mkt_trend_indicators`**
   - 使用 IntermediateCache
   - ext_indicators 不再包含 `_m_shape_h`

2. **验证**
   - 实时运行
   - 重启恢复
   - 区间查询

### 阶段3：回填适配（1天）

1. **改造 `data_recovery.py`**
   - 回填使用 IntermediateCache
   - 确保回填和实时一致

---

## 九、待审核确认

```
□ 存储分离：累积型历史独立存储，ext_indicators固定大小
□ MySQL单表通用：day_history_intermediate，indicator字段区分
□ Redis通用：day_hist:{indicator}:{date}，12小时过期
□ 四级缓存：L1内存→L2Redis→L3MySQL→L4重新计算
□ 区间查询：get_range返回list
□ 应用到_mkt_shape_history
□ 支持扩展其他指标（无需改表）
□ 实施计划（3天）

审核通过后，我将按阶段1→2→3逐步实施。
```

