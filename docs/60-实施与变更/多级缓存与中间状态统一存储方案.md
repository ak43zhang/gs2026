# 多级缓存与中间状态统一存储方案

**文档版本**: v3.0  
**生成时间**: 2026-07-26 04:20  
**状态**: 🟡 待审核

---

## 版本沿革

| 版本 | 时间 | 变更内容 | 状态 |
|------|------|----------|------|
| v1.0 | 2026-07-26 03:51 | 初始方案：新建 `ext_state` 字段存储中间状态，四级缓存架构 | 废弃 |
| v2.0 | 2026-07-26 04:03 | 优化：中间字段放入现有 `ext_indicators`，加 `_state_` 前缀，扁平化 JSON | 废弃 |
| **v3.0** | **2026-07-26 04:20** | **最终方案：JSON 与 DataFrame 同构，短 key，_m_前缀，个券状态分级存储（只存TOP30），适配回填，实时/回填逻辑统一，不影响现有逻辑** | **待审核** |

---

## 一、设计原则（v3.0 最终版）

| 原则 | 说明 |
|------|------|
| **同构存储** | JSON 结构与 DataFrame 同构，便于 `pd.DataFrame(json)` 直接解析 |
| **短 key** | 尽量缩短 key 长度，中间状态加 `_m_` 前缀便于识别 |
| **兼容现有** | 不影响原有计算逻辑，原有字段保持不变 |
| **零侵入** | 不阻塞主流程，状态恢复失败自动降级 |
| **实时/回填一致** | 同一套 `calculate_func`，同一套状态恢复逻辑 |
| **每 tick 存储** | 保持现状，每 tick 存储完整状态 |
| **四级缓存** | L1 内存 → L2 Redis → L3 MySQL → L4 重新计算 |

---

## 二、内存结构分析

### 2.1 现有中间变量内存结构

| 变量 | 内存结构 | 说明 |
|------|----------|------|
| `_mkt_shape_history` | `[{'t': int, 'p': float}, ...]` | 形态历史，dict 列表 |
| `_mkt_trend_slope_10m_cache` | `deque([(t, p), ...])` | 斜率缓存，tuple 队列 |
| `_mkt_ext_price_cache` | `deque([(t, p), ...])` | 扩展指标缓存 |
| `_mkt_trend_vwap_sum_pv/v` | `float` | 标量累积值 |
| `_mkt_trend_day_high/low` | `float` | 标量极值 |
| `_slope_buf_short` | `{code: deque([p1, p2, ...])}` | 个券斜率缓存 |

### 2.2 同构 JSON 设计

将内存结构直接映射为 JSON，保持同构：

```python
# 内存结构 → JSON 结构（同构）

# 1. 形态历史：list[dict] → list[dict]
_mkt_shape_history = [
    {'t': 32400, 'p': -0.5},
    {'t': 32700, 'p': -0.3},
    # ...
]
# JSON: 直接序列化
"mkt_shape_h": [{"t":32400,"p":-0.5}, {"t":32700,"p":-0.3}, ...]

# 2. 斜率缓存：deque[tuple] → list[list]
_mkt_slope_10m_cache = deque([(32400, -0.5), (32430, -0.3), ...])
# JSON: 转为二维数组
"mkt_s10m": [[32400,-0.5], [32430,-0.3], ...]

# 3. 标量值：float → number
_mkt_vwap_pv = 12345678.9
# JSON: 直接存储
"mkt_vwap_pv": 12345678.9
```

---

## 三、ext_indicators 字段设计（v3.0 短key版）

### 3.1 字段命名（尽量短）

```python
ext_indicators = {
    # ====== 结果字段（原有，保持简短） ======
    "mkt_vw_bias": -0.3,           # mkt_vwap_bias
    "mkt_ws10m": 0.05,             # mkt_weighted_slope_10m
    "mkt_pos": 65.5,               # mkt_day_position
    "mkt_nld": 5.2,                # mkt_new_low_distance
    "mkt_shp": "低开高走",          # mkt_shape
    "mkt_shd": "低点在开盘后15分钟", # mkt_shape_detail
    "ws2m": 0.02,                  # weighted_slope_2m
    "ws5m": 0.03,                  # weighted_slope_5m
    "c1m": 1.2,                    # change_1m_pct
    "pacc": 0.01,                  # price_acceleration
    "m_ws2m": 0.04,                # mkt_weighted_slope_2m
    "m_c1m": 0.8,                  # mkt_change_1m_pct
    "m_pacc": 0.02,                # mkt_price_acceleration
    
    # ====== 中间状态字段（新增，_m_前缀便于识别） ======
    # 大盘VWAP状态（标量）
    "_m_vwap_pv": 12345678.9,       # _mkt_trend_vwap_sum_pv
    "_m_vwap_v": 987654321.0,        # _mkt_trend_vwap_sum_v
    "_m_vwap": 0.0125,               # 当前VWAP值
    
    # 大盘日内极值（标量）
    "_m_high": 1.52,                 # _mkt_trend_day_high
    "_m_low": -0.85,                # _mkt_trend_day_low
    "_m_h_t": 34200,                # high时间（秒）
    "_m_l_t": 33600,                # low时间（秒）
    
    # 大盘创新低时间（标量/None）
    "_m_nld_t": 33600,              # _mkt_trend_last_new_low_time
    
    # 形态历史（同构：list[dict]）
    "_m_shape_h": [                  # _mkt_shape_history
        {"t": 32400, "p": -0.5},
        {"t": 32700, "p": -0.3},
        {"t": 33000, "p": 0.0},
        # ... 全天数据
    ],
    
    # 10分钟斜率缓存（同构：list[list]）
    "_m_s10m": [                    # _mkt_trend_slope_10m_cache
        [33900, 0.8],
        [33930, 0.82],
        [33960, 0.85],
        # ... 最近500点
    ],
    
    # 大盘扩展指标缓存（同构：list[list]）
    "_m_ep": [                       # _mkt_ext_price_cache
        [33900, 0.5],
        [33930, 0.52],
        # ... 最近300点
    ],
    
    # 个券级状态（按债券编码扁平存储，_m_前缀）
    # 格式: "{bond_code}_m_{field}": value
    "123456_m_ss": [1.2, 1.3, 1.4],    # bond_code + _m_ss (slope_short)
    "123456_m_sl": [1.0, 1.1, 1.2],    # _m_sl (slope_long)
    "123456_m_ep": [[32400,105.5],...],# _m_ep (ext_price_cache)
    "123456_m_pamt": 5000000,           # _m_pamt (peak_vol_max_amount)
    "123456_m_pprc": 105.5,             # _m_pprc (peak_vol_price)
    "123456_m_hmax": 2.5,               # _m_hmax (high_max_cpct)
    
    # 元数据（_m_前缀）
    "_m_ver": "3.0",                # state_version
    "_m_t": "09:30:15",             # saved_time
    "_m_tc": 100,                    # tick_count
}
```

### 3.2 字段命名规则

| 类型 | 前缀 | 示例 | 说明 |
|------|------|------|------|
| 大盘结果 | `mkt_` | `mkt_vw_bias` | 大盘级结果指标（原有，不变） |
| 个券结果 | 无前缀 | `ws2m`, `c1m` | 个券级结果指标（原有，不变） |
| 大盘中间状态 | `_m_` | `_m_vwap_pv` | 大盘级中间状态（新增） |
| 个券中间状态 | `{code}_m_` | `123456_m_ss` | 个券级中间状态（新增） |
| 元数据 | `_m_` | `_m_ver`, `_m_t` | 版本、时间等（新增） |

**设计意图**：
- `_m_` 前缀 = "memory state"，明确标识中间状态
- 与结果字段区分，便于识别和过滤
- 短key减少存储空间

### 3.3 同构优势

```python
# 解析回 DataFrame（形态历史示例）
import json
ext = json.loads(df['ext_indicators'].iloc[0])

# 直接转为 DataFrame（同构，无需转换）
df_shape = pd.DataFrame(ext['_m_shape_h'])
# 列名: t, p
# 可直接用于计算

# 斜率缓存转为 DataFrame
df_slope = pd.DataFrame(ext['_m_s10m'], columns=['t', 'p'])

# 个券状态直接提取
code = '123456'
slope_short = ext[f'{code}_m_ss']  # 列表
```

---

## 四、四级缓存架构（查上一 tick）

### 4.1 缓存层级

```python
# L1: 内存缓存（进程内）
_prev_tick_cache = {
    't': None,      # time_full '09:30:12'
    'df': None,     # DataFrame（完整上一tick）
}

# L2: Redis（同机器）
# key: "monitor_zq_sssj_{date}:{time}"

# L3: MySQL（持久化）
# table: monitor_zq_sssj_{date}

# L4: 重新计算（兜底）
# 从当日历史tick重新计算
```

### 4.2 查询函数

```python
def get_prev_tick(table: str, time_full: str) -> Optional[pd.DataFrame]:
    """
    获取上一tick数据（四级缓存）
    顺序: L1内存 → L2Redis → L3MySQL → L4None
    """
    global _prev_tick_cache
    
    # L1: 内存命中（<1μs）
    if _prev_tick_cache['t'] == time_full:
        return _prev_tick_cache['df'].copy()
    
    # L2: Redis（1-5ms）
    df = redis_util.load_dataframe_by_offset(table, offset=0)
    if df is not None:
        _update_l1(df, time_full)
        return df
    
    # L3: MySQL（10-50ms）
    df = _load_from_mysql(table, time_full)
    if df is not None:
        _update_l1(df, time_full)
        return df
    
    # L4: 无数据
    return None

def _update_l1(df: pd.DataFrame, time_full: str):
    """更新L1缓存"""
    global _prev_tick_cache
    _prev_tick_cache['t'] = time_full
    _prev_tick_cache['df'] = df.copy()
```

### 4.3 性能对比

| 场景 | 改造前 | 改造后 | 提升 |
|------|--------|--------|------|
| 内存命中 | - | <1μs | 新增 |
| Redis查询 | 5-20ms | 5-20ms（降级） | - |
| 平均耗时 | 10ms | <1μs（L1命中率高） | **10000x** |

---

## 五、状态恢复（实时/回填统一）

### 5.1 统一恢复函数

```python
def recover_state_from_ext(ext: dict) -> bool:
    """
    从 ext_indicators 恢复中间状态
    
    用途：
    1. 实时：服务重启后恢复
    2. 回填：每个tick恢复上一tick状态
    """
    global _mkt_trend_vwap_sum_pv, _mkt_trend_vwap_sum_v
    global _mkt_trend_day_high, _mkt_trend_day_low
    global _mkt_shape_history, _mkt_trend_slope_10m_cache
    
    try:
        # 恢复标量状态
        _mkt_trend_vwap_sum_pv = float(ext.get('_m_vwap_pv', 0))
        _mkt_trend_vwap_sum_v = float(ext.get('_m_vwap_v', 0))
        _mkt_trend_day_high = float(ext.get('_m_high', -999))
        _mkt_trend_day_low = float(ext.get('_m_low', 999))
        
        # 恢复形态历史（同构：list[dict] → list[dict]）
        shape_h = ext.get('_m_shape_h', [])
        _mkt_shape_history = [{"t": d['t'], "p": d['p']} for d in shape_h]
        
        # 恢复斜率缓存（同构：list[list] → deque[tuple]）
        s10m = ext.get('_m_s10m', [])
        _mkt_trend_slope_10m_cache.clear()
        for item in s10m:
            _mkt_trend_slope_10m_cache.append(tuple(item))
        
        logger.info(f"[状态恢复] VWAP={_mkt_trend_vwap_sum_pv:.0f}, 形态历史={len(_mkt_shape_history)}点")
        return True
    except Exception as e:
        logger.warning(f"[状态恢复] 失败，从0开始: {e}")
        return False
```

### 5.2 实时流程（重启后）

```python
def compute_mkt_trend(df_now, time_full, date, df_prev=None):
    global _mkt_trend_date
    
    # 日期切换或首次运行 → 恢复状态
    if _mkt_trend_date != date:
        if df_prev is not None:
            # 从上一tick的ext_indicators恢复
            ext = json.loads(df_prev['ext_indicators'].iloc[0])
            recover_state_from_ext(ext)
        else:
            # 无上一tick，从0开始
            _init_state()  # 初始化所有状态为0
        _mkt_trend_date = date
    
    # 正常计算...
    # ... 更新状态 ...
    
    # 返回结果（包含中间状态，_m_前缀）
    return {
        # 结果字段（原有，不变）
        "mkt_vw_bias": mkt_vwap_bias,
        "mkt_ws10m": mkt_weighted_slope_10m,
        # ...
        
        # 中间状态字段（新增，_m_前缀）
        "_m_vwap_pv": _mkt_trend_vwap_sum_pv,
        "_m_vwap_v": _mkt_trend_vwap_sum_v,
        "_m_shape_h": [{"t": h['t'], "p": h['p']} for h in _mkt_shape_history],
        "_m_s10m": [list(item) for item in _mkt_trend_slope_10m_cache],
        # ...
    }
```

### 5.3 回填流程（data_recovery.py）

```python
# 回填时复用同一套计算函数
def backfill_calculate(df_now, df_prev, date, time, loop_start):
    """
    回填计算函数（与实时共用核心逻辑）
    """
    # 从df_prev恢复状态（同实时，_m_前缀）
    if df_prev is not None and 'ext_indicators' in df_prev.columns:
        ext = json.loads(df_prev['ext_indicators'].iloc[0])
        recover_state_from_ext(ext)
    
    # 调用实时计算逻辑（完全一致）
    result = compute_mkt_trend(df_now, time, date, df_prev)
    
    # 存储结果
    df_now['ext_indicators'] = json.dumps(result, ensure_ascii=False)
    save_dataframe(df_now, table, time, expire)

# data_recovery.py 中使用
calculate_func = backfill_calculate
recover_data(date, 'bond', calculate_func=calculate_func)
```

---

## 六、个券中间状态设计说明（回答疑问）

### 6.1 个券中间状态是什么？

| 变量 | 业务逻辑 | 用途 |
|------|----------|------|
| `_slope_buf_short` | 个券短周期斜率缓存(60点) | 计算 `weighted_slope_2m` |
| `_slope_buf_long` | 个券长周期斜率缓存(300点) | 计算 `weighted_slope_10m` |
| `_ext_price_cache` | 个券扩展指标价格缓存(300点) | 计算 `price_acceleration` |
| `_ext_slope_cache` | 个券上周期斜率 | 计算斜率变化率 |
| `_peak_vol_state` | 个券放量高点状态 | 计算 `peak_vol_bias` |
| `_high_state` | 个券日内高点状态 | 计算 `high_distance` |

### 6.2 为什么需要个券中间状态？

**场景**：服务重启后，个券指标计算失真

```
例：债券 123456 的 weighted_slope_2m

正常流程（连续运行）：
  tick1: 缓存 [1.0] → slope = 0
  tick2: 缓存 [1.0, 1.2] → slope = 0.2
  tick3: 缓存 [1.0, 1.2, 1.3] → slope = 0.15
  ...

重启后（无状态恢复）：
  tick1: 缓存 [] → slope = 0（错误！应为0.15）
  tick2: 缓存 [1.3] → slope = 0（错误！）
  需要60个tick才能恢复正常
```

**影响**：
- 斜率指标重启后约10-15分钟才能稳定
- `peak_vol_bias`、`high_distance` 等日内极值指标完全错误

### 6.3 动态 code 作为 key 的设计问题

**你的质疑是对的！** 动态 code 作为 key 有以下问题：

| 问题 | 说明 | 严重程度 |
|------|------|----------|
| **存储膨胀** | 400只债券 × 每条约7KB = 每tick 3MB | 🔴 高 |
| **Redis key 爆炸** | 400个债券 × N个字段 = 数千个key | 🔴 高 |
| **查询低效** | 需要遍历所有 code 前缀的key | 🟡 中 |
| **维护困难** | code 变动（退市/上市）导致key失效 | 🟡 中 |

**存储大小估算**：
```
单债券状态：
  - slope_short: 60 × 8B = 480B
  - slope_long: 300 × 8B = 2400B
  - ext_price_cache: 300 × 16B = 4800B
  - peak_vol_state: 2 × 8B = 16B
  - high_state: 1 × 8B = 8B
  ≈ 7.7 KB/债券

400只债券：400 × 7.7KB = 3 MB/tick

每3秒一个tick，一天4小时：
  4800 tick × 3MB = 14.4 GB/天（仅个券状态）
```

### 6.4 重新设计：个券状态分级处理

**核心原则**：大盘状态必须存，个券状态选择性存

| 级别 | 存储策略 | 原因 |
|------|----------|------|
| **大盘级** | ✅ 必须存储 | 影响所有债券，无法从个券反推 |
| **个券级-活跃** | ⚠️ 可选存储 | 只存TOP30活跃债券，减少90%存储 |
| **个券级-普通** | ❌ 不存储 | 10-15分钟自恢复，影响可接受 |

**优化后的存储结构**：

```python
ext_indicators = {
    # ====== 大盘级中间状态（必须存） ======
    "_m_vwap_pv": 12345678.9,
    "_m_vwap_v": 987654321.0,
    "_m_shape_h": [...],
    "_m_s10m": [...],
    # ...
    
    # ====== 个券级中间状态（只存TOP30活跃） ======
    # 格式: "_m_{code}_{field}": value
    "_m_123456_ss": [1.2, 1.3, ...],     # TOP30活跃债券
    "_m_123456_sl": [1.0, 1.1, ...],
    "_m_123456_pamt": 5000000,
    # ... 其他TOP29只
    
    # 普通债券不存个券状态，重启后10-15分钟自恢复
}
```

**存储优化效果**：
```
原方案：400只 × 7.7KB = 3 MB/tick
优化后：30只 × 7.7KB = 231 KB/tick（减少92%）
日存储：14.4GB → 1.1GB
```

### 6.5 个券状态恢复策略

```python
def recover_bond_state(ext: dict, code: str):
    """
    恢复个券状态（分级策略）
    """
    # 尝试从ext恢复（只存了TOP30）
    ss_key = f"_m_{code}_ss"
    sl_key = f"_m_{code}_sl"
    
    if ss_key in ext:
        # 有存储，直接恢复
        _slope_buf_short[code] = deque(ext[ss_key], maxlen=60)
        _slope_buf_long[code] = deque(ext[sl_key], maxlen=300)
        logger.debug(f"[个券恢复] {code} 从存储恢复")
    else:
        # 无存储，从0开始（10-15分钟自恢复）
        _slope_buf_short[code] = deque(maxlen=60)
        _slope_buf_long[code] = deque(maxlen=300)
        logger.debug(f"[个券恢复] {code} 从0开始（非活跃债券）")
```

### 6.6 设计利弊总结

| 方面 | 原设计（存全部） | 优化设计（只存TOP30） |
|------|----------------|---------------------|
| **存储** | 3MB/tick，14GB/天 | 231KB/tick，1.1GB/天 |
| **恢复精度** | 100% | TOP30: 100%，其他: 10-15分钟恢复 |
| **业务影响** | 无 | 非活跃债券10-15分钟指标略不准 |
| **复杂度** | 高（400个key） | 低（30个key） |
| **可维护性** | 差 | 好 |

**结论**：
- ✅ **大盘级状态**：必须存储（影响全局）
- ⚠️ **个券级状态**：只存TOP30活跃债券（减少92%存储，业务影响可控）
- ❌ **普通个券**：不存储（10-15分钟自恢复，可接受）

---

## 七、实施计划

### 阶段1：基础函数（1天）

1. **新增 `get_prev_tick` 函数**
   - 实现 L1→L2→L3→L4 四级查询
   - L1 内存缓存机制

2. **新增 `recover_state_from_ext` 函数**
   - 从 ext_indicators 解析中间状态
   - 大盘级：全部恢复
   - 个券级：只恢复TOP30活跃债券

### 阶段2：核心改造（1天）

1. **改造 `compute_mkt_trend_indicators`**
   - 接收 `df_prev` 参数
   - 调用 `recover_state_from_ext`
   - 返回大盘级 `_m_` 中间状态

2. **改造 `compute_ext_indicators`**
   - 返回个券级 `_m_` 中间状态（只存TOP30）

3. **改造主循环**
   - 使用 `get_prev_tick` 替代直接查Redis
   - 更新 L1 缓存

### 阶段3：回填适配（1天）

1. **新建 `backfill_calculate` 函数**
   - 包装实时计算函数
   - 确保回填和实时逻辑一致

2. **改造 `data_recovery.py`**
   - 使用新的 `backfill_calculate`
   - 验证回填结果与实时一致

### 阶段4：测试验证（1天）

1. **单元测试**
   - 四级缓存查询
   - 状态恢复准确性

2. **集成测试**
   - 模拟重启，验证状态恢复
   - 对比实时和回填结果

3. **性能测试**
   - L1 缓存命中率
   - 查上一tick耗时
   - 存储大小验证

---

## 七、风险与缓解

| 风险 | 缓解 |
|------|------|
| ext_indicators 膨胀 | 只存关键状态，缓存截断（最近100-500点） |
| 解析失败 | try/except 包裹，降级从0开始，不影响主流程 |
| 版本不兼容 | `_m_ver` 字段标记版本，不兼容则重置 |
| 内存占用 | L1只缓存1个DataFrame，约10MB |
| 实时/回填不一致 | 共用 `recover_state_from_ext` 和 `compute_mkt_trend` |
| 影响现有逻辑 | 原有计算逻辑不变，新增状态字段不影响效率 |

---

## 八、待审核确认

```
□ JSON 与 DataFrame 同构存储（list[dict], list[list]）
□ 短 key + _m_ 前缀便于识别中间状态
□ 个券状态分级存储：大盘级全存，个券级只存TOP30活跃债券
□ 不影响原有计算逻辑，原有字段保持不变
□ 实时/回填共用 recover_state_from_ext 和 compute_mkt_trend
□ 四级缓存：L1内存 → L2Redis → L3MySQL → L4重新计算
□ 实施计划（4天）

审核通过后，我将按阶段1→2→3→4逐步实施。
```

---

**文档结束**
