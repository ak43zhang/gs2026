# 提示精简方案 v2.0

> **版本**: v2.0  
> **日期**: 2026-07-28  
> **状态**: 待审核  
> **需求**: 只保留00:00-09:25提示，集合竞价和15:00后都不提示

---

## 当前提示

| 时段 | 当前提示 |
|------|----------|
| 00:00-09:25 | ⏸️ 非交易时段（00:00-09:25），09:30自动恢复刷新 |
| 09:25-09:30 | ✅ 集合竞价大盘概览已加载... / 📡 集合竞价加载大盘概览中... |
| 15:00后 | ⏸️ 已收盘（15:00后），点击时间轴查看历史 |

---

## 修改方案

只保留 `case 'night'`，其他 `case` 都 `show = false`：

```javascript
switch (phase) {
    case 'night':
        // 只保留这个提示
        text = '⏸️ 非交易时段（00:00-09:25），09:30自动恢复刷新';
        show = true;
        break;
    case 'auction':
        show = false;  // 集合竞价不提示
        break;
    case 'trading':
        show = false;  // 交易时段不提示
        break;
    case 'closed':
        show = false;  // 15:00后不提示
        break;
}
```

---

## 修改后行为

| 时段 | 是否显示提示 | 提示内容 |
|------|-------------|----------|
| **00:00-09:25** | **✅ 显示** | **⏸️ 非交易时段（00:00-09:25），09:30自动恢复刷新** |
| 09:25-09:30 | ❌ 不显示 | - |
| 09:30-15:00 | ❌ 不显示 | - |
| 15:00后 | ❌ 不显示 | - |

---

**确认后实施。**
