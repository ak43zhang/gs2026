# 数据监控时间轴双源查询设计文档

**文档版本**: v1.0  
**创建日期**: 2026-04-18  
**功能模块**: 数据监控 - 时间轴  
**相关文件**: 
- `src/gs2026/dashboard/services/data_service.py`
- `src/gs2026/dashboard2/routes/monitor.py`

---

## 一、需求背景

### 当前问题
| 问题 | 说明 |
|------|------|
| 只查 Redis | `get_timestamps()` 方法仅查询 Redis `timestamps` 列表 |
| 历史数据无时间轴 | Redis 数据过期（默认72小时）后，历史日期返回空列表 |
| 用户体验差 | 选择历史日期时，时间轴滑块无法使用 |

### 索引检查结果
- ✅ `time` 字段已有索引 `idx_time`
- ✅ DISTINCT 查询耗时约 45-67ms，性能良好
- ✅ 无需额外索引优化

---

## 二、设计方案

### 2.1 核心设计：Redis + MySQL 双源查询

```
查询流程:
1. 先查 Redis timestamps 列表
2. 如果 Redis 有数据 → 返回 Redis 数据（实时性好）
3. 如果 Redis 无数据 → 查 MySQL 获取时间点列表
4. 如果 MySQL 有数据 → 返回 MySQL 数据（支持历史数据）
5. 如果都无数据 → 返回空列表
```

### 2.2 MySQL 查询逻辑

```sql
SELECT DISTINCT time FROM monitor_gp_apqd_{date} ORDER BY time
```

**性能**: 利用现有 `idx_time` 索引，查询耗时 < 100ms

### 2.3 代码实现

#### 修改文件: `data_service.py`

```python
def get_timestamps(self, date: Optional[str] = None, use_mysql: bool = True) -> List[str]:
    """
    获取指定日期的所有数据时间点
    优先 Redis，Redis 无数据则回退到 MySQL
    
    Args:
        date: 日期 YYYYMMDD，默认今天
        use_mysql: 是否允许查询 MySQL 回退
    
    Returns:
        时间点列表（已排序），如 ['09:30:00', '09:30:03', ...]
    """
    if date is None:
        date = self.get_latest_date()
    
    # 1. 先查 Redis
    if self.redis_available:
        try:
            client = redis_util._get_redis_client()
            # 股票和债券的时间戳应该一致，优先查股票
            ts_key = f"monitor_gp_apqd_{date}:timestamps"
            all_ts = client.lrange(ts_key, 0, -1)
            
            if all_ts:
                timestamps = sorted(set(
                    t.decode('utf-8') if isinstance(t, bytes) else t
                    for t in all_ts
                ))
                return timestamps
        except Exception as e:
            print(f"Redis 获取 timestamps 失败: {e}")
    
    # 2. Redis 无数据，回退到 MySQL
    if use_mysql:
        try:
            # 查询股票表的时间点
            table_name = f"monitor_gp_apqd_{date}"
            sql = f"SELECT DISTINCT time FROM {table_name} ORDER BY time"
            
            with self.engine.connect() as conn:
                df = pd.read_sql(sql, conn)
            
            if not df.empty:
                timestamps = df['time'].tolist()
                return timestamps
        except Exception as e:
            print(f"MySQL 获取 timestamps 失败: {e}")
    
    return []
```

---

## 三、实施步骤

| 步骤 | 内容 | 文件 | 风险 |
|------|------|------|------|
| 1 | 修改 `get_timestamps` 方法 | `data_service.py` | 低 |
| 2 | 验证 Redis 优先逻辑 | 测试 | 低 |
| 3 | 验证 MySQL 回退逻辑 | 测试 | 低 |

---

## 四、兼容性

| 方面 | 处理 |
|------|------|
| 默认行为 | `use_mysql=True`，自动回退，不影响现有功能 |
| 性能 | MySQL 查询使用 `idx_time` 索引，<100ms |
| 错误处理 | Redis/MySQL 异常都不影响，返回空列表 |
| 其他模块 | 无影响，仅修改 `get_timestamps` 方法 |

---

## 五、验证方式

1. **实时数据**: 选择今天，时间轴应从 Redis 获取（快速）
2. **历史数据**: 选择3天前的日期，时间轴应从 MySQL 获取
3. **无数据**: 选择无数据的日期，时间轴为空，滑块不可用

---

*文档创建时间: 2026-04-18 14:41*
