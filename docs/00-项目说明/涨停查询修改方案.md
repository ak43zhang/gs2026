# 涨停查询修改方案

## 问题分析

用户反馈："查询结果不是涨停的股票，而是涉及到行业和概念的股票"

**实际含义：** 用户期望看到**所有涨停股票**，但当前实现只返回**匹配标签的涨停股票**。

**举例说明：**
- 14:30有100只涨停股票
- 用户选择"半导体"标签
- **当前实现**：只返回涨停且属于半导体的股票（比如20只）
- **用户期望**：返回所有100只涨停股票，但高亮显示属于半导体的

## 修改方案

### 方案A：不筛选，只高亮（推荐）

**逻辑：**
1. 获取指定时间点的所有涨停股票（100只）
2. 计算每只股票匹配的标签
3. **返回所有涨停股票**，但标记哪些匹配了选中标签
4. 前端高亮显示匹配的标签

**修改：**

```python
def filter_ztb_snapshot(date: str, time: str, selected_tags: List[Dict], filters: Dict) -> Dict:
    # 1. 获取指定时间点的所有涨停股票
    df = _get_zt_stocks_at_time(date_str, time)
    
    # 2. 从宽表缓存获取行业和概念
    stock_codes = df['stock_code'].astype(str).tolist()
    stock_details = _get_stock_details_batch(stock_codes)
    
    # 3. 【修改】不筛选，只计算匹配情况
    selected_industries_set = set(t['name'] for t in selected_tags if t['type'] == 'industry')
    selected_concepts_set = set(t['name'] for t in selected_tags if t['type'] == 'concept')
    
    stock_matches = {}
    for code in stock_codes:
        details = stock_details.get(code, {})
        all_industries = set(details.get('industry_names', []))
        all_concepts = set(details.get('concepts', []))
        
        matched_industries = list(selected_industries_set & all_industries)
        matched_concepts = list(selected_concepts_set & all_concepts)
        
        # 【修改】不跳过，所有涨停股票都返回
        stock_matches[code] = {
            'stock_name': details.get('stock_name', ''),
            'bond_code': details.get('bond_code', '-'),
            'bond_name': details.get('bond_name', '-'),
            'all_industries': list(all_industries),
            'matched_industries': matched_industries,  # 可能为空
            'matched_concepts': matched_concepts,      # 可能为空
            'match_count': len(matched_industries) + len(matched_concepts)
        }
    
    # 4. 【修改】按match_count分组，0匹配的也显示
    # ...
```

### 方案B：按匹配度分组展示

**逻辑：**
- 匹配全部标签的（比如同时匹配"半导体+芯片"）
- 匹配部分标签的（只匹配"半导体"）
- 不匹配任何标签的（其他涨停股票）

**分组：**
1. 命中全部标签（比如命中2个）
2. 命中1个标签
3. 命中0个标签（其他涨停股票）

### 方案C：保持现状，但优化展示

**逻辑：**
- 只返回匹配标签的涨停股票
- 但明确提示"显示涨停中属于半导体的股票"

## 推荐方案A

理由：
1. 用户选择时间点，期望看到该时间点的完整涨停情况
2. 选择标签是为了**筛选关注**，不是**过滤掉其他**
3. 与默认模式（交叉选股）逻辑区分：
   - 交叉选股：查询所有有标签的股票
   - 涨停行概：查询涨停股票，高亮标签

## 实施步骤

1. 修改`filter_ztb_snapshot`，不跳过不匹配的股票
2. 按match_count分组，0匹配的单独一组
3. 前端展示所有分组，包括"其他涨停"

---

等待用户确认方案。
