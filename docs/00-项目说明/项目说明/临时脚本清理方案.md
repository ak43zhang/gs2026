# 临时脚本清理方案

## 需要清理的临时脚本

### 1. docs目录整理相关脚本
| 脚本路径 | 用途 | 状态 |
|----------|------|------|
| `scripts/huatai_trader/reorganize_docs.py` | docs目录整理（第一阶段） | 已完成，可删除 |
| `scripts/huatai_trader/reorganize_docs_full.py` | docs目录完整整理 | 已完成，可删除 |
| `scripts/huatai_trader/clean_docs.py` | 清理旧docs目录 | 已完成，可删除 |
| `scripts/huatai_trader/rename_to_chinese.py` | 重命名英文文件 | 已完成，可删除 |
| `scripts/huatai_trader/rename_deep.py` | 深度重命名 | 已完成，可删除 |
| `scripts/huatai_trader/list_english_files.py` | 列出英文文件 | 已完成，可删除 |
| `scripts/huatai_trader/verify_migration.py` | 验证文件迁移 | 已完成，可删除 |
| `scripts/huatai_trader/migrate_missing.py` | 迁移遗漏文件 | 已完成，可删除 |

### 2. 其他临时脚本（如有）
- 需要检查 `scripts/` 目录下是否有其他临时脚本

---

## 清理方案

### 方案A：直接删除（推荐）
直接删除上述8个临时脚本，它们都是docs整理专用，任务已完成。

### 方案B：移动到归档目录
将临时脚本移动到 `scripts/archive/` 或 `docs/12-历史归档/` 中保留。

### 方案C：保留但重命名
将脚本重命名为 `*.bak` 或移动到 `scripts/temp/` 目录。

---

## 建议

推荐**方案A（直接删除）**，原因：
1. 这些脚本都是一次性使用的docs整理工具
2. 任务已完成，脚本已无保留价值
3. 如需恢复，可以从git历史找回
4. 保持目录整洁，避免混淆

---

## 清理后目录结构

```
scripts/
├── huatai_trader/
│   ├── server.py              # 交易助手服务（保留）
│   ├── trader.py              # 交易核心（保留）
│   ├── trader_adapter.py      # 适配器（保留）
│   ├── test_e2e.py            # 端到端测试（保留）
│   ├── test_tdx_source.py     # TDX数据源测试（保留）
│   └── DESIGN.md              # 设计文档（保留）
└── (临时脚本已清理)
```

---

## 实施命令

```bash
# 删除临时脚本
cd F:\pyworkspace2026\gs2026\scripts\huatai_trader
del reorganize_docs.py
del reorganize_docs_full.py
del clean_docs.py
del rename_to_chinese.py
del rename_deep.py
del list_english_files.py
del verify_migration.py
del migrate_missing.py

# Git提交
cd F:\pyworkspace2026\gs2026
git add -A
git commit -m "chore: 清理docs整理临时脚本"
```

---

**审核状态**: 待审核
