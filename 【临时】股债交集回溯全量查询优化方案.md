# 股债交集回溯 - 全量查询优化方案（分批流式 + code过滤）

**分析日期**: 2026-08-04  
**状态**: 待审核  
**背景**: 卡在 preload_sssj('stock')——股票sssj全天约250万行一次性加载，慢且占内存

---

## 一、瓶颈精确定位

| 表 | 全天数据量 | 是否瓶颈 |
|----|-----------|---------|
| top30（股/债） | 债券2985行，股票约≤13万行 | ❌ 小，一次加载即可 |
| **sssj（股票）** | **约250万行** | ✅ **唯一瓶颈** |
| sssj（债券） | 约30万行 | 次要 |

结论：**只需对 sssj 分批**，top30 一次性加载没问题。

---

## 二、两个关键优化点

### 优化点1：code过滤（大幅砍行数）
- sssj（实时数据表）记录**全市场每只股票每个tick**的数据
- 但回溯**只关心 top30 出现过的 code**（约570只，非全市场5000只）
- 加 `WHERE code IN (top30候选codes)` → 直接砍掉全市场无关股票

### 优化点2：分批流式（你的思路，按tick分批）
- 每批 N 个 tick（默认100），只加载该批时间窗口的 sssj
- 每批：`WHERE code IN (候选) AND time BETWEEN 批开始 AND 批结束`
- 处理完即释放内存，内存有界

---

## 三、完整方案设计

### 3.1 整体流程

```
【一次性加载 top30】（小表，快）
  ├─ 构建全部tick的 count时间线（累计，从开盘算）
  ├─ 构建全部tick的 window_count时间线
  └─ 得到候选code集合（只有这些code需要sssj）

【按tick分批处理】（每批100个tick）
  for 批 in 分批(timestamps, 每批100):
      批开始, 批结束 = 批[0], 批[-1]
      # 只加载这批时间窗口 + 候选code 的 sssj
      stock_sssj = SELECT ... WHERE code IN(候选) AND time BETWEEN 批开始 AND 批结束
      bond_sssj  = 同理
      for tick in 批:
          从count/window_count时间线切片 + sssj切片 组装
          过滤（UnifiedPipeline，逻辑不变）
          交集 → 累加results
      释放本批sssj内存

【返回】所有results
```

### 3.2 为什么 count/window_count 时间线可以一次性构建
- top30表小（十几万行），一次加载无压力
- count是累计值，必须从开盘连续算 → 一次性构建最简单可靠
- window_count只看当前10分钟窗口 → 一次性构建也无压力
- 分批只针对 sssj（大表）

### 3.3 code过滤的威力
- 假设 sssj 全市场5000只 × 4428tick = 2200万行（实际250万可能已是部分）
- 候选只570只 → `code IN` 后约 570 × 4428 = 250万 → **仍需分批**
- 但配合分批：每批100tick × 570只 = **5.7万行/批**，秒级

### 3.4 分批查询次数
| 场景 | 批数 | sssj查询次数 |
|------|------|-------------|
| 全天(4428tick, 每批100) | ~44批 | ~44×2 = 88次 |
| 窄窗口(如20tick) | 1批 | 2次 |

对比原来逐tick：4428×2 = 8856次 → **降到88次**

---

## 四、内存与性能预估

| 指标 | 原逐tick | 我上版(全量) | 本方案(分批+code过滤) |
|------|---------|-------------|---------------------|
| 峰值内存 | 低 | ~150MB(全天) | **~10MB(每批)** |
| sssj查询 | 8856次 | 2次(但单次巨大) | 88次(每次小) |
| 全天耗时 | 数分钟 | 加载卡死 | **预计5~15秒** |
| 窄窗口 | 秒级 | 秒级 | **亚秒级** |

**分批方案在"全天"和"内存"上都最优**，是全量查询的正解。

---

## 五、逻辑一致性保证（不变）

1. count/window_count 时间线一次性构建，与现有 `_get_*_window_count_batch` 算法一致
2. 每个tick仍是"组装→谓词过滤→排行过滤→交集"，`UnifiedPipeline` **零改动**
3. 分批只改变"sssj取数的时机"，不改变任何字段计算与过滤逻辑
4. change_pct/amount/main_net 字段来源、绿名单标记 全部不变

---

## 六、可选增强（本次可不做）

- **SSE流式进度**：每批处理完向前端推送进度条（"已处理 12/44 批"）。当前先做同步返回，若全天仍嫌久再加。

---

## 七、实施步骤

1. [ ] `preload_top30` 保持一次性加载（已支持end裁剪），额外返回候选code集合
2. [ ] `preload_sssj` 增加 `codes` 参数（`WHERE code IN`）
3. [ ] `api_run` 改为按tick分批循环：每批加载sssj → 处理 → 释放
4. [ ] 分批大小可配（默认100，作为常量 `BATCH_TICKS = 100`）
5. [ ] 写验证脚本：分批版 vs 旧逐tick版，交集结果 diff=0
6. [ ] 性能实测（全天 + 窄窗口）

**工作量**：约2~3小时

---

## 八、需你确认

1. 采用**分批(每批100 tick) + code过滤(IN top30候选)**方案？
2. 批大小默认100，可接受？（也可设50/200，影响进度粒度与查询次数）
3. 本次先做**同步返回**（不做SSE流式进度），全天若仍久再加进度推送——可以吗？

---

**等待审核**
