# 股债交集回溯 - 性能优化方案

**分析日期**: 2026-08-04  
**问题**: 遍历时间范围内所有tick太慢  
**状态**: 待用户审核

---

## 一、当前慢的根因（代码实测得出，非猜测）

当前 `backtrace.py` 是**逐tick循环**，每个时间点都做一遍完整查询链路：

```
for time_str in timestamps:   # 全天约4428个时间点！
    stock_data = _get_ranking_fast('stock', ...)          # 排行
    stock_data = _enrich_stock_data(...)                  # 补bond_code/industry
    stock_data = _enrich_change_pct_and_main_net(...)     # 查sssj涨跌幅+主力净额
    bond_data  = _get_ranking_fast('bond', ...)           # 排行
    bond_data  = _enrich_bond_data(...)                   # 查sssj涨跌幅/金额/行业
    过滤 + 交集
```

### 三个致命性能点

**① 每tick重建MySQL连接池（最严重）**
`_get_change_pct_from_mysql` 里：
```python
engine = create_engine(Config.MYSQL_URI)  # 每次调用都新建引擎+连接池！
```
4428个tick × 多次调用 = 上万次建池，光这一项就拖垮性能。

**② 每tick单独查sssj表**
- sssj表按 `time` 存全天数据，行数约 = 股票数×时间点数 ≈ 570×4428 ≈ **250万行**
- 每tick按 `WHERE time = :t` 查一次，4428个tick就是4428次往返
- Redis未命中时全部落到MySQL单点查询

**③ `_get_ranking_fast` 增量计数器状态被反复打断**
- 该函数用模块级全局 `_rank_incremental` 做增量：时间前进时只查增量(~10ms)
- 但回溯循环中股票、债券交替调用，且时间严格递增才走快路径
- 首次/后退会触发全量 `GROUP BY`（~300ms）

---

## 二、优化方案（核心思路：一次性批量算，内存里遍历）

你的理解完全正确——**可以一起算出来**。sssj表本身就是全时段数据，一次全量拉取，在内存里按时间点切片即可，无需逐tick查库。

### 方案总览

```
【一次性预加载】(2~4次大查询，全部在循环外)
  ├─ 股票排行时间线：一次GROUP BY拿到每个code的累计count随时间变化
  ├─ 债券排行时间线：同上
  ├─ 股票sssj全量：一次查全天 → 按time分组成 {time: {code: change_pct/main_net}}
  ├─ 债券sssj全量：一次查全天 → 按time分组成 {time: {code: change_pct/amount}}
  ├─ 股债映射：一次性get_cache (已缓存)
  └─ 绿名单集合：一次性get (已实现)

【内存遍历】(无DB访问)
  for time_str in timestamps:
      从预加载的dict里切片 → 组装stock_data/bond_data → 过滤 → 交集
```

### 关键优化点

| 优化项 | 当前 | 优化后 |
|--------|------|--------|
| MySQL引擎 | 每tick create_engine | 复用_get_shared_engine（1个） |
| sssj查询 | 4428次(每tick一次) | **2次**(股票/债券各一次全量) |
| 排行count | 每tick查/增量 | **1次**全量按时间线累计 |
| 涨跌幅/金额 | 每tick查 | 内存dict切片(0次DB) |
| 数据传输 | 4428×往返延迟 | 2×大批量 |

---

## 三、具体实现设计

### 3.1 排行count时间线（一次算完）

sssj表/top30表按time递增。一次查询拿到"每个时间点每个code的累计count"：

```python
# 一次性查询：按时间聚合的累计count
# top30表每行是"某code在某time上榜一次"，累计count = 该code截至time的行数
sql = f"SELECT code, name, time FROM monitor_gp_top30_{date} ORDER BY time"
rows = conn.execute(sql).fetchall()

# 内存里构建：每个时间点的累计count快照
# 用增量方式：遍历一次，维护running_counter
timeline = {}          # {time_str: {code: count}}
counter = {}           # running累计
row_idx = 0
for t in sorted_timestamps:
    while row_idx < len(rows) and rows[row_idx].time <= t:
        code = rows[row_idx].code
        counter[code] = counter.get(code, 0) + 1
        row_idx += 1
    timeline[t] = dict(counter)   # 该时点的快照
```

> 注：window_count（区间次数）也可用同样方式一次算出。

### 3.2 sssj全量预加载（一次查，内存切片）

```python
# 股票涨跌幅/主力净额：一次全量
sql = f"SELECT stock_code, time, change_pct, main_net_amount FROM monitor_gp_sssj_{date}"
df = pd.read_sql(sql, engine)
# 按time分组成嵌套dict
stock_sssj = {}   # {time: {code: {'change_pct':x, 'main_net':y}}}
for t, g in df.groupby('time'):
    stock_sssj[t] = g.set_index('stock_code')[['change_pct','main_net_amount']].to_dict('index')

# 债券同理
sql = f"SELECT bond_code, time, change_pct, amount FROM monitor_zq_sssj_{date}"
```

### 3.3 内存遍历组装（0次DB）

```python
# 预加载股债映射、绿名单（循环外）
mapping = get_cache()          # bond_code/industry
green_set = _get_green_bond_set(date)

for time_str in timestamps:
    # 从时间线切片
    stock_counts = rank_timeline_stock.get(time_str, {})
    stock_sssj_slice = stock_sssj.get(time_str, {})
    # 组装stock_data（内存操作）
    stock_data = []
    for code, cnt in stock_counts.items():
        m = mapping.get(code, {})
        sssj = stock_sssj_slice.get(code, {})
        stock_data.append({
            'code': code, 'count': cnt,
            'bond_code': m.get('bond_code','-'),
            'industry_name': m.get('industry',''),
            'change_pct': sssj.get('change_pct'),
            'main_net_amount': sssj.get('main_net_amount'),
            'is_green_bond': ...
        })
    # 债券同理 → 过滤 → 交集
```

---

## 四、预期收益

| 指标 | 当前 | 优化后 | 提升 |
|------|------|--------|------|
| DB查询次数 | ~4428×5 ≈ 2万+ | ~4次 | **5000倍** |
| create_engine | 上万次 | 0次 | ∞ |
| 全天回溯耗时 | 数分钟 | **预计1~5秒** | 数十~百倍 |
| 窄窗口(如1分钟) | 秒级 | 亚秒级 | — |

---

## 五、实施要点与风险

### 实施要点
1. 新增 `_preload_timeline(date, asset_type)` — 一次算排行count时间线
2. 新增 `_preload_sssj(date, asset_type)` — 一次拉sssj全量分组
3. 改造 `api_run` 循环体：从预加载dict切片组装，不再调enrich函数
4. 保留原enrich逻辑的**字段计算规则**（确保结果与现有一致）

### 风险与对策
| 风险 | 对策 |
|------|------|
| 内存占用（250万行） | 只SELECT必要列(4~5列)；股票sssj约250万行×5列≈几十MB，可接受 |
| 结果与现有不一致 | 用相同date+time对比新旧两版交集结果，必须完全一致才切换 |
| window_count计算 | 沿用现有区间匹配逻辑，一次算出时间线 |
| Redis vs MySQL差异 | 统一走MySQL全量（回溯是历史数据，不依赖Redis实时性） |

### 验证方案
1. 选一个时间点，新旧两版分别算交集，diff必须为0
2. 全天回溯计时对比（优化前vs优化后）
3. 边界：空窗口、单tick、绿名单过滤后仍一致

---

## 六、工作量

| 任务 | 预计 |
|------|------|
| 预加载函数（排行时间线+sssj） | 2小时 |
| 改造循环体 | 1小时 |
| 结果一致性验证 | 1小时 |
| **合计** | **约4小时** |

---

## 需你确认

1. 是否接受"统一走MySQL全量预加载"（回溯是历史数据，不走Redis实时缓存）？
2. window_count（区间次数）是否也需要一次性算时间线？（当前若用到需一并优化）
3. 内存方案可接受（峰值几十MB），还是要分批？

---

**等待审核**
