# 股债交集回溯 - 顶部导航栏统一方案

**分析日期**: 2026-08-04  
**状态**: 待审核  
**问题**: backtrace 页面顶部导航栏与交叉行概选股/区间测算等页面不一致

---

## 一、根因定位（代码实测）

### 1.1 两种完全不同的导航加载机制

| 页面 | 顶部导航机制 |
|------|-------------|
| stock_picker.html（交叉行概选股） | **JS动态生成**：`<div id="nav-placeholder">` + `nav.js` 的 `initNav('picker')` |
| range_analysis.html（区间测算） | **JS动态生成**：同上，`initNav('picker')` |
| **backtrace.html（股债交集回溯）** | **Jinja服务端include**：`{% include 'nav.html' %}` ❌ 不一致 |

### 1.2 具体差异

**其他页面（正确做法）**:
```html
<body>
    <!-- 顶部导航占位，由nav.js统一填充 -->
    <div id="nav-placeholder"></div>
    ...
    <script src="{{ url_for('static', filename='js/nav.js') }}"></script>
    <script>initNav('picker');</script>
</body>
```

**backtrace.html（当前错误做法）**:
```html
<body>
    {% set page_title = '智能选股' %}
    {% set active_page = 'picker' %}
    {% include 'nav.html' %}       <!-- ❌ 用了服务端include，与其他页面机制不同 -->
    ...
</body>
```

### 1.3 为什么长得不一样

- `nav.js` 的 `initNav()` 动态生成的是**统一的顶部主导航栏**（首页/监控/智能选股/...），带统一样式、logo、响应式
- `nav.html` 是另一套（可能是旧的或不同结构的）导航模板
- 两者 HTML 结构和 CSS 类不同 → 视觉不一致

---

## 二、nav.js 机制说明

`initNav(activeKey)` 的工作方式：
1. 找到 `<div id="nav-placeholder">`
2. 注入统一的顶部导航HTML（含所有主菜单项）
3. 根据 `activeKey` 高亮当前菜单

`activeKey` 取值（实测 nav.js 中定义）：
- `'picker'` → 智能选股（stock_picker、range_analysis 都用这个）

**结论**：backtrace 属于智能选股子页，应该用 `initNav('picker')`，与区间测算完全一致。

---

## 三、修改方案

### 3.1 改动点（backtrace.html 顶部）

**删除**（当前的服务端include）:
```html
{% set page_title = '智能选股' %}
{% set active_page = 'picker' %}
{% include 'nav.html' %}
```

**替换为**（与其他页面一致的JS动态导航）:
```html
<div id="nav-placeholder"></div>
```

### 3.2 改动点（backtrace.html 底部，`</body>` 前）

**新增**（引入nav.js并初始化）:
```html
<script src="{{ url_for('static', filename='js/nav.js') }}"></script>
<script>initNav('picker');</script>
```

### 3.3 CSS确认

需确认 backtrace.html 是否已引入 nav.js 依赖的 CSS。其他页面（如 range_analysis）引入了哪些 base CSS，backtrace 需对齐（当前已引入 base.css/components.css，应该够，实施时核对）。

### 3.4 子导航（智能选股内部tab）保留

backtrace.html 现有的**子导航**（🎯交叉行概选股 / 📈涨停行概选股 / 📊区间测算 / 🔍股债交集回溯）是页面内的二级tab，**保留不动**，只改最顶部的主导航栏。

> 注：需确认其他子页（range_analysis）的子导航是否也是页面内自己写的。如果子导航也有统一机制，一并对齐；如果各页自己写，则保持现状。

---

## 四、实施步骤

1. [ ] backtrace.html 顶部：`{% include 'nav.html' %}` 三行 → `<div id="nav-placeholder"></div>`
2. [ ] backtrace.html 底部：新增 `nav.js` 引入 + `initNav('picker')`
3. [ ] 核对 CSS 依赖（base.css/components.css/nav相关）与 range_analysis 一致
4. [ ] 验证：顶部导航栏与交叉行概选股/区间测算视觉完全一致，高亮"智能选股"
5. [ ] 验证：子导航（页面内tab）正常，股债交集回溯高亮

---

## 五、风险评估

| 风险 | 说明 | 对策 |
|------|------|------|
| CSS缺失 | nav.js生成的HTML可能依赖特定CSS | 对齐range_analysis的CSS引入 |
| 子导航冲突 | 主导航改了可能影响子导航布局 | 子导航独立，不受影响 |
| activeKey错误 | 高亮不对 | 用'picker'与区间测算一致 |

改动极小（顶部3行→1行 + 底部加2行），风险低。

---

## 六、需你确认

1. 采用 `nav.js` + `initNav('picker')` 方案，与区间测算完全对齐？
2. 页面内的子导航（�Stan交叉行概选股/涨停/区间测算/股债交集回溯 tab）保留不动，对吗？

---

**等待审核**
