# 股债交集回溯 - 根因诊断与修复方案（数据实测版）

**诊断日期**: 2026-08-04  
**方法**: 真实数据实测（`_diag_backtrace.py`），不猜测

---

## 一、实测发现的根本问题

### 1.1 数据字段实测结果（8月3日 09:53:57）

**股票表 `monitor_gp_top30_20260803` 字段**：
```
code, name, price_now, volume_now, amount_now, change_pct_now,
price_prev, ..., zf_30, momentum, ..., total_score, rq, time,
main_net_amount, window_count
```

**债券表 `monitor_zq_top30_20260803` 字段**：与股票表结构相同。

**000703 原始记录**（能查到）：
```
code=000703, name=恒逸石化, change_pct_now=0.12, count(隐含),
main_net_amount=0.00, window_count=11, time=09:53:24
```

### 1.2 关键结论

| 发现 | 说明 |
|------|------|
| **股票表无 `bond_code` 字段** | 股债关联靠**外部映射表**（`get_cache()` 股债映射缓存），不在行情表里 |
| **股票表无 `industry` 字段** | 行业信息同样来自映射缓存的 `industry_name` |
| **表内涨跌幅是 `change_pct_now`** | 但前端展示的 `change_pct` 来自 `monitor_gp_sssj` 表（另一张表） |
| **`get_ranking_at_time` 只返回 7 字段** | `code/name/count/type/date/rank/window_count`——**缺 bond_code/industry/change_pct/amount** |

### 1.3 为什么回溯"无数据"

我的 backtrace 直接调 `data_service.get_ranking_at_time()`：
1. 返回的股票数据**没有 `bond_code`** → `IntersectionCalculator` 靠 `stock.bond_code == bond.code` 关联 → **交集永远为空**
2. 债券数据**没有 `amount`** → 前N金额过滤失效
3. 都**没有 `industry`** → 行业过滤失效

**这就是 000703 明明能命中条件，但回溯无数据的根因。**

---

## 二、正确的数据链路（前端实际使用）

前端"股票上攻排行"走 `/api/monitor/attack-ranking/stock`，其内部链路：

```
_get_ranking_fast('stock', date, time_str, limit)   # code/name/count/rank
  ↓
_enrich_stock_data(data, date, time_str)             # +bond_code +industry_name +is_green_bond +window_count
  ↓
_enrich_change_pct_and_main_net(data, date, time_str) # +change_pct +main_net_amount
  ↓
标记红名单 is_red
```

债券侧走 `/api/monitor/attack-ranking/bond`：
```
_get_ranking_fast('bond', date, time_str, limit)     # code/name/count/rank
  ↓
_enrich_bond_data(data, date, query_time)            # +change_pct +industry +price +amount
  ↓
标记绿名单 is_green
```

**结论**：回溯必须复用这套 enrich 函数，而不是直接调 data_service。

---

## 三、修复方案

### 3.1 核心改动：backtrace.py 复用 monitor.py 的 enrich 函数

```python
from gs2026.dashboard2.routes.monitor import (
    _get_ranking_fast,
    _enrich_stock_data,
    _enrich_change_pct_and_main_net,
    _enrich_bond_data,
)

def _get_enriched_stock(date, time_str):
    data = _get_ranking_fast('stock', date, time_str, 0)
    data = _enrich_stock_data(data, date, time_str)      # 补 bond_code/industry_name
    data = _enrich_change_pct_and_main_net(data, date, time_str)  # 补 change_pct
    return data

def _get_enriched_bond(date, time_str):
    data = _get_ranking_fast('bond', date, time_str, 0)
    data = _enrich_bond_data(data, date, time_str)       # 补 change_pct/industry/amount
    return data
```

### 3.2 字段名对齐

前端过滤器 `filters.py` 用的字段 vs enrich 后的真实字段：

| 过滤器 | filters.py 用字段 | enrich 后真实字段 | 是否一致 |
|--------|-------------------|-------------------|----------|
| 转债模式 | `bond_code` | `bond_code` (值'-'表示无) | ✅ |
| 行业次数前N | `industry` | `industry_name` | ❌ 需改 |
| 前N金额 | `amount` | 需确认 enrich 后字段名 | 待验证 |
| 前N区间次数 | `window_count` | `window_count` | ✅ |
| 隐藏绿名单 | `is_green`/`is_green_bond` | 股票`is_green_bond`/债券`is_green` | ❌ 需统一 |

### 3.3 前端配置字段名对齐

前端 `backtrace.js` 传的 config 字段与后端 `FilterConfig.from_dict` 期望的**完全不一致**：

| 前端传 | 后端期望 |
|--------|----------|
| `bond_mode` | `stock_bond_filter` |
| `topn_industry` | `stock_topn_sectors` |
| `topn_industry_pct` | `stock_topn_sectors_pct` |
| `topn_window` | `stock_topn_window` |
| `green_list` | `bond_green_list` |
| `topn_amount` | `bond_topn_amount` |

**必须统一命名**。

### 3.4 新增开始/结束时间

```python
start_time = data.get('start_time')
end_time = data.get('end_time')
timestamps = get_timestamps(date)
if start_time:
    timestamps = [t for t in timestamps if t >= start_time]
if end_time:
    timestamps = [t for t in timestamps if t <= end_time]
```

### 3.5 谓词/排行模式支持

前端每个"前N"过滤器有 predicate/ranking 两种模式，后端 `FilterConfig` 需要为每个过滤器增加 mode 字段，`filters.py` 的 RankingFilter 需支持 predicate 模式。

---

## 四、验证方案（先验证再交付）

写独立验证脚本 `_verify_backtrace.py`：
1. 用8月3日09:53:57 + 转债模式，验证股票数据带 `bond_code`
2. 验证 000703 是否出现在结果中
3. 验证交集计算能产出数据
4. 打印每一步的数据量（原始→enrich→过滤→交集）

**只有验证脚本能命中000703并产出交集，才算修复完成。**

---

## 五、实施顺序

1. [ ] 对齐 `FilterConfig` 字段名（前端↔后端）
2. [ ] `filters.py` 行业过滤器改用 `industry_name`
3. [ ] `filters.py` 支持 predicate 模式
4. [ ] `backtrace.py` 复用 monitor enrich 函数
5. [ ] `backtrace.py` 新增 start_time/end_time
6. [ ] 前端 `backtrace.html`/`backtrace.js` 新增时间区间 + 谓词/排行切换
7. [ ] 写 `_verify_backtrace.py` 实测验证命中000703
8. [ ] 全链路验证通过后提交

---

**等待用户确认后实施**
