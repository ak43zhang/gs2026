# 股债交集回溯 - 完整性能优化方案（v2 · 字段确认版）

**分析日期**: 2026-08-04  
**状态**: 待用户审核  
**前提**: 已确认统一走 MySQL 全量预加载；逻辑严格沿用现有设计（谓词→排行）

---

## 一、字段定义确认（代码实测，逐一核对）

### 1.1 count（累计次数）
- **来源**: top30表（`monitor_gp_top30_{date}` / `monitor_zq_top30_{date}`）
- **定义**: 某code截至 `time_str` 在top30表出现的**总行数**（累计上榜次数）
- **实测**: 300996 在 ≤14:29:00 的 count = **120**
- **算法**: `COUNT(*) WHERE time <= time_str GROUP BY code`

### 1.2 window_count（区间次数）
- **来源**: top30表的 **`window_count` 列**（不是COUNT计算，是表里的字段值）
- **定义**: 当前10分钟窗口内**最新一条记录**的 window_count 列值；窗口内无记录则为 **0**
- **窗口**: `window_start = HH:(mm//10*10):00` 到 `time_str`
- **实测**: 300996 最新记录在 11:21:39，14:29:00 时其窗口(14:20~14:29)无记录 → window_count = **0**（但 count=120）
- **算法**（现有 `_get_*_window_count_batch`）:
  ```sql
  SELECT t1.code, t1.window_count FROM top30 t1
  INNER JOIN (SELECT code, MAX(time) max_time FROM top30
              WHERE time >= window_start AND time <= time_str
              GROUP BY code) t2
  ON t1.code=t2.code AND t1.time=t2.max_time
  ```

### 1.3 change_pct（涨跌幅）
- **来源**: sssj表（`monitor_gp_sssj_{date}` / `monitor_zq_sssj_{date}`）的 `change_pct` 列
- **定义**: 该code在 `time = time_str` 那一刻的涨跌幅；无则 '-'

### 1.4 main_net_amount（股票主力净额）
- **来源**: sssj表 `main_net_amount` 列（time=time_str）

### 1.5 amount（债券金额）、price（债券价格）
- **来源**: 债券sssj表 `amount` / `price` 列（time=time_str）

### 1.6 bond_code / industry_name（股票关联转债与行业）
- **来源**: 股债映射缓存 `get_cache().get_mappings_smart(codes)`（**已内存缓存，非DB**）

### 1.7 is_green / is_green_bond（绿名单标记）
- **来源**: 绿名单集合（当天Redis / 历史MySQL），**已在上次实现**

---

## 二、当前慢的根因（已确认）

逐tick循环，全天约4428个时间点，每tick执行：

| 每tick操作 | DB查询 | 问题 |
|-----------|--------|------|
| `_get_ranking_fast` ×2(股/债) | 增量或全量GROUP BY | 交替调用打断增量 |
| `_get_change_pct_batch` ×2 | sssj按time=? 查 | 4428次单点查询 |
| `_get_*_window_count_batch` ×2 | top30 JOIN 查 | 4428次JOIN查询 |
| `_get_change_pct_from_mysql` | **create_engine每次新建** | 上万次建连接池 |

**总计**: 约 4428 × 6 ≈ **2.6万次DB查询** + 上万次建池。

---

## 三、优化方案（一次性加载 → 内存遍历，逻辑不变）

### 核心原则
**逻辑严格沿用现有设计**：
1. 每个时间点仍然是"组装完整数据 → 谓词过滤 → 排行过滤 → 交集"
2. `UnifiedPipeline`（谓词各自独立取交集 → 排行基于候选池S）**完全不动**
3. 只把"逐tick查库"换成"一次性查全量 + 内存切片"，字段计算规则100%一致

### 3.1 循环外一次性预加载（5次大查询）

```python
# ① 股票top30全量（用于count累计 + window_count时间线）
#    SELECT code,name,time,window_count FROM monitor_gp_top30_{date} ORDER BY time
# ② 债券top30全量（同上）
# ③ 股票sssj全量：SELECT stock_code,time,change_pct,main_net_amount FROM monitor_gp_sssj_{date}
# ④ 债券sssj全量：SELECT bond_code,time,change_pct,amount,price FROM monitor_zq_sssj_{date}
# ⑤ 股债映射 get_cache（已缓存）+ 绿名单集合（已实现）
```

全部用 `_get_shared_engine()`（复用连接池，**0次新建**）。

### 3.2 内存预计算三个时间线

**A. count 累计时间线**（遍历top30行，running累加）
```python
# rows按time排序，指针扫描
count_timeline = {}   # {time: {code: 累计count}}
counter = {}
idx = 0
for t in timestamps:
    while idx < len(rows) and rows[idx].time <= t:
        counter[rows[idx].code] = counter.get(rows[idx].code, 0) + 1
        idx += 1
    count_timeline[t] = dict(counter)   # 快照
```

**B. window_count 时间线**（每code在每个10分钟窗口的最新值）
```python
# 对每个 (code, window_start) 取窗口内 MAX(time) 的 window_count 列值
# 预处理：把top30行按 (code, 归属窗口) 分组，取窗口内最新记录的window_count
# 遍历时: 若code在当前窗口有记录取该值，否则0（与现有逻辑一致）
```

**C. sssj 切片字典**（按time分组）
```python
stock_sssj = {}   # {time: {code: {'change_pct':x,'main_net_amount':y}}}
bond_sssj  = {}   # {time: {code: {'change_pct':x,'amount':y,'price':z}}}
```

### 3.3 内存遍历组装（0次DB）

```python
mapping_all = cache.get_mappings_smart(all_stock_codes)  # 一次性
for t in timestamps:
    # 股票组装
    counts = count_timeline_stock[t]
    wc = window_count_timeline_stock[t]
    sssj = stock_sssj.get(t, {})
    stock_data = []
    for code, cnt in counts.items():
        m = mapping_all.get(code, {})
        s = sssj.get(code, {})
        bc = m.get('bond_code', '-')
        stock_data.append({
            'code': code, 'name': ..., 'count': cnt,
            'window_count': wc.get(code, 0),
            'bond_code': bc,
            'industry_name': m.get('industry_name', '-'),
            'change_pct': s.get('change_pct', '-'),
            'main_net_amount': s.get('main_net_amount', 0),
            'is_green_bond': bc != '-' and str(bc).zfill(6) in green_set,
        })
    # 债券组装（同理，含 is_green）
    bond_data = [...]
    # 过滤（逻辑完全不变）
    fs = stock_pipeline.filter_stocks(stock_data)
    fb = bond_pipeline.filter_bonds(bond_data)
    inter = IntersectionCalculator.calculate(fs, fb)
```

### 3.4 谓词→排行顺序保证
- 组装出的 `stock_data`/`bond_data` 字段与现有enrich产出**完全一致**
- 交给**同一个** `UnifiedPipeline.filter_stocks/filter_bonds`
- 谓词各自独立取交集 → 排行基于候选池S → 完全沿用现有逻辑，**零改动**

---

## 四、预期收益

| 指标 | 当前 | 优化后 |
|------|------|--------|
| DB查询次数 | ~2.6万次 | **5次** |
| create_engine | 上万次 | 0次 |
| 全天回溯 | 数分钟 | **预计1~5秒** |

---

## 五、内存评估

| 数据 | 量级 | 内存 |
|------|------|------|
| 股票sssj(4~5列) | ~250万行 | ~80MB |
| 债券sssj | ~30万行 | ~10MB |
| top30(股+债) | ~50万行 | ~15MB |
| count/wc时间线快照 | 4428×活跃code | 峰值~50MB |
| **合计峰值** | — | **~150MB**（可接受） |

> 若担心内存，可选优化：count时间线不存全量快照，改存"变更增量"，遍历时按需重建（降到~20MB）。**建议先用全量快照版（简单可靠），内存不够再优化。**

---

## 六、验证方案（结果必须与现有版完全一致）

1. **一致性验证**（关键）:
   - 选3个时间点（如09:35:00 / 11:20:00 / 14:29:00）
   - 新版预加载版 vs 旧版逐tick版，分别算交集
   - 逐字段diff（stock_code/bond_code/change_pct/count/window_count/amount）
   - **必须完全一致才切换**
2. **性能验证**: 全天回溯计时，优化前后对比
3. **边界验证**: 空窗口、单tick、绿名单过滤后一致、谓词/排行切换后一致

---

## 七、实施步骤

1. [ ] 新增 `_preload_top30(date, asset_type)` → 返回 count时间线 + window_count时间线
2. [ ] 新增 `_preload_sssj(date, asset_type)` → 返回 {time: {code: fields}}
3. [ ] 改造 `api_run`：循环外预加载，循环内内存组装（复用现有Pipeline）
4. [ ] 保留旧逐tick逻辑为 `_run_legacy`（对比验证用，验证通过后可删）
5. [ ] 写验证脚本，新旧diff=0
6. [ ] 性能实测 + 提交

**工作量**: 约4~5小时

---

## 需你最终确认

1. window_count 定义我已确认（top30表列值，取当前10分钟窗口内最新记录值，无则0）——是否正确？
2. count 定义（累计上榜次数）——是否正确？
3. 内存方案：先用**全量快照版**（简单，~150MB），可接受吗？
4. 是否保留旧版 `_run_legacy` 做对比（验证后再删）？

---

**等待审核**
