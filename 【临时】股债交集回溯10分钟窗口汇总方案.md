# 股债交集回溯 - 10分钟窗口汇总方案

**需求日期**: 2026-08-04  
**状态**: 待审核  

---

## 一、当前数据结构

```json
{
  "time": "14:29:00",
  "count": 5,
  "stocks": [
    {
      "code": "000703",
      "name": "恒逸石化",
      "bond_code": "127067",
      "bond_name": "恒逸转2",
      "industry_name": "化学纤维",
      "change_pct": 0.37,
      "window_count": 3
    }
  ]
}
```

**问题**: 返回的是每个tick的明细，数据量大，难以看出趋势。

---

## 二、10分钟窗口汇总方案

### 2.1 窗口定义

- **窗口大小**: 10分钟（与 window_count 的10分钟窗口对齐）
- **窗口边界**: 09:30, 09:40, 09:50, 10:00, 10:10...
- **窗口标识**: `09:30-09:40`, `09:40-09:50`...

### 2.2 汇总维度

按 **股票+债券对** 汇总，一个窗口内同一对只出现一次。

### 2.3 汇总字段设计

| 字段 | 说明 | 计算方式 |
|------|------|----------|
| `stock_code` | 股票代码 | 原始值 |
| `stock_name` | 股票名称 | 原始值 |
| `bond_code` | 债券代码 | 原始值 |
| `bond_name` | 债券名称 | 原始值 |
| `industry_name` | 行业 | 原始值 |
| `window_start` | 区间开始时间 | 窗口第一个tick时间 |
| `window_end` | 区间结束时间 | 窗口最后一个tick时间 |
| `appear_count` | **区间出现次数** | 该对在该窗口内出现的tick数 |
| `first_change_pct` | **区间开始涨幅** | 窗口内第一次出现的 change_pct |
| `max_change_pct` | **区间最高涨幅** | 窗口内该对的最大 change_pct |
| `min_change_pct` | 区间最低涨幅 | 窗口内该对的最小 change_pct |
| `last_change_pct` | 区间结束涨幅 | 窗口内最后一次的 change_pct |
| `avg_change_pct` | 区间平均涨幅 | 窗口内该对的平均 change_pct |

### 2.4 前端展示表格

| 股票 | 转债 | 行业 | 区间 | 出现次数 | 开始涨幅 | 最高涨幅 | 最低涨幅 | 结束涨幅 |
|------|------|------|------|----------|----------|----------|----------|----------|
| 000703 恒逸石化 | 127067 恒逸转2 | 化学纤维 | 14:20-14:30 | 5 | 0.37% | 1.25% | 0.30% | 1.10% |

---

## 三、可选扩展指标（供你选择）

| 指标 | 说明 | 价值 |
|------|------|------|
| `window_count_avg` | 区间平均window_count | 反映持续性 |
| `first_main_net` / `max_main_net` | 主力净流入 | 资金动向 |
| `price_change` | 转债价格变化 | 债端收益 |
| `amount_avg` | 区间平均成交金额 | 活跃度 |
| `consecutive_ticks` | 连续出现tick数 | 持续性指标 |
| `rank_avg` | 平均排名 | 强势程度 |

---

## 四、实施方式

### 方式A：后端汇总（推荐）
- 在 `_run_backtrace_core` 生成器内，按窗口聚合后再yield
- 前端改动小，只需改 `generateResultsHTML`

### 方式B：前端汇总
- 后端保持原样返回明细
- 前端 `displayResults` 时按窗口聚合
- 更灵活，但数据量大时性能差

**推荐方式A**，数据量小，用户体验好。

---

## 五、需你确认

1. 窗口大小固定10分钟？还是可配置（5/10/15/30分钟）？
2. 汇总字段是否足够？需要我列出的扩展指标吗？
3. 展示方式：表格（如上）还是卡片？需要图表吗？
4. 是否需要按窗口排序？还是按出现次数/涨幅排序？

---

**等待审核**
