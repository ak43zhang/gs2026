# 股票实时监控涨停字段优化设计

> 创建时间: 2026-04-22 12:52
> 版本: 1.0
> 相关文件: `src/gs2026/monitor/monitor_stock.py`

---

## 一、需求背景

在股票实时监控系统 (`monitor_gp_sssj_{date}` 表) 中增加涨停相关字段，用于：
1. 实时识别当前涨停的股票
2. 追踪当天曾经涨停的股票
3. 为后续涨停分析和选股提供数据基础

---

## 二、新增字段说明

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `is_zt` | TINYINT(1) | 0 | 是否涨停: 1=当前是涨停状态, 0=当前不是涨停状态 |
| `ever_zt` | TINYINT(1) | 0 | 曾经涨停: 1=当天有过涨停, 0=当天没有涨停 |

---

## 三、涨停判断规则

### 3.1 A股涨停幅度规则

| 板块 | 涨停幅度 | 代码特征 |
|------|----------|----------|
| 沪深主板 | ±10% | 600/601/603/605/000/001/002/003开头 |
| 科创板 | ±20% | 688开头 |
| 创业板 | ±20% | 300/301开头 |
| 北交所 | ±30% | 8/9开头 |
| ST股票 | ±5% | 股票名称含ST/*ST |

### 3.2 涨停判断逻辑

```python
def get_zt_limit(code: str, name: str = None) -> float:
    """获取股票的涨停幅度限制"""
    # ST股票判断
    if name and ('ST' in name or '*ST' in name):
        return 5.0
    
    # 根据代码前缀判断
    if code.startswith('688'):  # 科创板
        return 20.0
    elif code.startswith(('300', '301')):  # 创业板
        return 20.0
    elif code.startswith(('8', '9')):  # 北交所
        return 30.0
    else:  # 沪深主板
        return 10.0


def calc_is_zt(change_pct: float, code: str, name: str = None) -> int:
    """计算是否涨停"""
    if pd.isna(change_pct):
        return 0
    
    zt_limit = get_zt_limit(code, name)
    # 涨停判断：涨跌幅 >= 涨停幅度 - 0.1 (考虑精度误差)
    return 1 if change_pct >= (zt_limit - 0.1) else 0
```

---

## 四、数据库变更

### 4.1 表结构变更SQL

```sql
-- 为 monitor_gp_sssj_2026 表增加涨停字段
ALTER TABLE monitor_gp_sssj_2026 
ADD COLUMN is_zt TINYINT(1) DEFAULT 0 COMMENT '是否涨停: 1=是, 0=否',
ADD COLUMN ever_zt TINYINT(1) DEFAULT 0 COMMENT '曾经涨停: 1=当天有过涨停, 0=没有';

-- 添加索引（用于快速筛选涨停股票）
CREATE INDEX idx_is_zt ON monitor_gp_sssj_2026(is_zt);
CREATE INDEX idx_ever_zt ON monitor_gp_sssj_2026(ever_zt);
CREATE INDEX idx_time_is_zt ON monitor_gp_sssj_2026(time, is_zt);
```

### 4.2 动态表处理

由于表名是动态的（`monitor_gp_sssj_{date_str}`），需要在首次写入时自动添加字段：

```python
def ensure_zt_columns(table_name: str):
    """确保表中有涨停相关字段"""
    sql = f"""
    ALTER TABLE {table_name} 
    ADD COLUMN IF NOT EXISTS is_zt TINYINT(1) DEFAULT 0 COMMENT '是否涨停: 1=是, 0=否',
    ADD COLUMN IF NOT EXISTS ever_zt TINYINT(1) DEFAULT 0 COMMENT '曾经涨停: 1=当天有过涨停, 0=没有'
    """
    # 执行SQL...
    
    # 添加索引
    index_sql = f"""
    CREATE INDEX IF NOT EXISTS idx_is_zt ON {table_name}(is_zt);
    CREATE INDEX IF NOT EXISTS idx_ever_zt ON {table_name}(ever_zt);
    CREATE INDEX IF NOT EXISTS idx_time_is_zt ON {table_name}(time, is_zt)
    """
    # 执行SQL...
```

---

## 五、代码实现

### 5.1 涨停判断工具函数

```python
# ========== 涨停判断工具函数 ==========

# 内存缓存：记录当天曾经涨停的股票代码
_ever_zt_cache: Set[str] = set()
_ever_zt_cache_date: str = ""


def get_zt_limit(code: str, name: str = None) -> float:
    """获取股票的涨停幅度限制"""
    if name and ('ST' in name or '*ST' in name):
        return 5.0
    
    if code.startswith('688'):  # 科创板
        return 20.0
    elif code.startswith(('300', '301')):  # 创业板
        return 20.0
    elif code.startswith(('8', '9')):  # 北交所
        return 30.0
    else:  # 沪深主板
        return 10.0


def calc_is_zt(change_pct: float, code: str, name: str = None) -> int:
    """计算是否涨停"""
    if pd.isna(change_pct):
        return 0
    
    zt_limit = get_zt_limit(code, name)
    return 1 if change_pct >= (zt_limit - 0.1) else 0


def update_ever_zt_cache(date_str: str, zt_codes: Set[str]):
    """更新曾经涨停缓存"""
    global _ever_zt_cache, _ever_zt_cache_date
    
    if date_str != _ever_zt_cache_date:
        _ever_zt_cache.clear()
        _ever_zt_cache_date = date_str
    
    _ever_zt_cache.update(zt_codes)


def is_ever_zt(code: str, date_str: str) -> int:
    """判断股票当天是否曾经涨停"""
    global _ever_zt_cache, _ever_zt_cache_date
    
    if date_str != _ever_zt_cache_date:
        _ever_zt_cache.clear()
        _ever_zt_cache_date = date_str
    
    return 1 if code in _ever_zt_cache else 0
```

### 5.2 修改数据处理流程

在 `deal_gp_works` 函数中增加涨停字段计算：

```python
def deal_gp_works(loop_start):
    """单个轮询周期的主处理函数"""
    date_str = loop_start.strftime('%Y%m%d')
    time_full = loop_start.strftime("%H:%M:%S")

    # 获取股票数据
    df_now = fetch_all_concurrently(STOCK_CODES)
    if not df_now.empty:
        df_now['stock_code'] = df_now['stock_code'].astype(str).str.zfill(6)

    df_now['time'] = time_full

    # ========== 新增：计算涨停字段 ==========
    if not df_now.empty:
        # 转换 change_pct 为数值
        df_now['change_pct'] = pd.to_numeric(df_now['change_pct'], errors='coerce')
        
        # 计算是否涨停
        df_now['is_zt'] = df_now.apply(
            lambda row: calc_is_zt(
                row.get('change_pct'), 
                row.get('stock_code', ''),
                row.get('short_name', '')
            ), 
            axis=1
        )
        
        # 更新曾经涨停缓存
        zt_codes = set(df_now[df_now['is_zt'] == 1]['stock_code'].tolist())
        update_ever_zt_cache(date_str, zt_codes)
        
        # 计算是否曾经涨停
        df_now['ever_zt'] = df_now['stock_code'].apply(
            lambda code: is_ever_zt(code, date_str)
        )

    # 存储数据
    sssj_table = f"monitor_gp_sssj_{date_str}"
    ensure_zt_columns(sssj_table)
    save_dataframe(df_now, sssj_table, time_full, EXPIRE_SECONDS)
```

---

## 六、使用示例

### 6.1 查询当前涨停股票

```sql
SELECT stock_code, short_name, change_pct, time
FROM monitor_gp_sssj_20260422
WHERE is_zt = 1
ORDER BY time DESC;
```

### 6.2 查询当天曾经涨停的股票

```sql
SELECT DISTINCT stock_code, short_name
FROM monitor_gp_sssj_20260422
WHERE ever_zt = 1;
```

### 6.3 查询某只股票的涨停历史

```sql
SELECT time, change_pct, is_zt, ever_zt
FROM monitor_gp_sssj_20260422
WHERE stock_code = '000001'
ORDER BY time;
```

---

## 七、性能影响

| 指标 | 影响 | 说明 |
|------|------|------|
| 计算性能 | 极小 | 涨停判断为简单数值计算 |
| 存储空间 | 小 | 每行增加2字节 |
| 内存占用 | 小 | 只缓存涨停股票代码 |
| 查询性能 | 提升 | 可通过索引快速筛选 |

---

## 八、风险评估

| 风险 | 等级 | 应对措施 |
|------|------|----------|
| 性能影响 | 低 | 涨停判断为简单数值计算 |
| 数据一致性 | 低 | 新增字段，不影响现有数据 |
| 代码兼容性 | 低 | 新增可选字段，向后兼容 |

---

## 九、Git提交记录

| Commit | 说明 |
|--------|------|
| `9ccba09` | chore: 保存当前工作进度，准备实施涨停字段优化 |

---

## 十、实施状态

- [x] 设计方案完成
- [x] 代码保存到Git
- [x] 设计文档创建
- [ ] 代码实施
- [ ] 测试验证
