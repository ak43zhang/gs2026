# 股债交集回溯 - 三个问题排查与修复方案

**排查日期**: 2026-08-04  
**状态**: 待用户审核

---

## 问题1：勾选隐藏绿名单，结果中仍有绿名单数据（如300996）

### 根因

绿名单标记 `is_green`（债券）/ `is_green_bond`（股票）**不是在 enrich 函数里做的**，而是在 `monitor.py` 的路由函数（`get_bond_ranking` / `get_stock_ranking`）里**单独用一段代码**标记的。

backtrace.py 复用了 enrich 函数，但**漏掉了绿名单标记这一步**，导致：
- 债券数据的 `is_green` 字段根本不存在（全是默认 False）
- `GreenListFilter` 判断 `is_green` 全为 False → 一个都不过滤

**证据**（monitor.py 的 get_bond_ranking）：
```python
data = _enrich_bond_data(data, actual_date, query_time)  # enrich不含绿名单
# 绿名单标记是独立一段：
cache_date = get_green_bond_list_cache_date()
if cache_date == actual_date:
    green_bond_list = get_green_bond_list()      # 当天：Redis
else:
    green_bond_list = <从MySQL green_bond_list表按buy_date查>  # 历史：MySQL
for item in data:
    item['is_green'] = item.get('code','') in green_bond_list
```

### 关于300996的语义澄清（重要）

300996 是**股票**（普联软件），它的转债是 123261。用户勾选的是**债券侧**"隐藏绿名单"。

需要明确过滤语义（两种可能，需你确认）：
- **语义A**：债券侧隐藏绿名单 = 在债券列表中剔除绿名单债券 → 若 123261 是绿名单，则债券侧不含 123261 → 交集里不会出现 300996↔123261
- **语义B**：只隐藏"债券自己"是绿名单的，与股票无关

我推断你要的是**语义A**：只要转债 123261 在绿名单，这条股债对就不该出现在交集里。

### 修复方案

在 backtrace.py 的 enrich 之后、过滤之前，**补上绿名单标记**（完全复刻 monitor.py 的逻辑，支持当天Redis/历史MySQL双数据源）：

```python
# 获取绿名单集合（当天Redis / 历史MySQL）
def _get_green_set(actual_date):
    from gs2026.dashboard2.routes.green_bond_list_cache import (
        get_green_bond_list, get_green_bond_list_cache_date)
    cache_date = get_green_bond_list_cache_date()
    if cache_date == actual_date:
        return get_green_bond_list()
    from gs2026.utils.mysql_util import get_mysql_tool
    import pandas as pd
    mysql_tool = get_mysql_tool()
    date_sql = f"{actual_date[:4]}-{actual_date[4:6]}-{actual_date[6:8]}"
    df = pd.read_sql(
        f"SELECT DISTINCT code FROM green_bond_list WHERE buy_date='{date_sql}'",
        con=mysql_tool.engine)
    return set(df['code'].astype(str).str.zfill(6).tolist()) if not df.empty else set()

green_set = _get_green_set(actual_date)  # 循环外获取一次

# 循环内 enrich 后标记：
for b in bond_data:
    b['is_green'] = str(b.get('code','')).zfill(6) in green_set
for s in stock_data:
    bc = str(s.get('bond_code','')).zfill(6)
    s['is_green_bond'] = (s.get('bond_code') not in (None,'-','')) and bc in green_set
```

这样：
- 债券侧隐藏绿名单 → `GreenListFilter` 正确剔除绿名单债券
- 股票侧同理（若股票也加隐藏绿名单）
- 采用语义A：转债在绿名单 → 债券被剔除 → 交集不含该股债对

---

## 问题2：股票过滤配置缺失

### 现状（backtrace.html 股票侧现有控件）
| 控件 | ID |
|------|-----|
| 仅显示有转债的股票 | stockBondFilter |
| 前N行业（次数） | stockTopNIndustry |
| 前N行业（涨幅） | stockTopNIndustryPct |
| 前N区间次数 | stockTopNWindow (+Mode) |

### 对照前端 monitor.html 股票完整过滤器（STOCK_PIPELINE）
1. 行业联动（industry）
2. 转债模式（bond_mode）✅ 已有
3. 仅行业次数前N（topn_sectors）✅ 已有
4. 仅行业涨幅前N（topn_sectors_pct）✅ 已有
5. 仅前N区间次数（topn_window）✅ 已有
6. **仅前N次数（topn_count）** ❌ 缺失

### 用户明确指出
- "前N区间次数应该叫**仅前区间次数**"（命名对齐前端）
- "还缺少**仅前N次数**"

### 修复方案（股票侧）
1. 新增控件 `stockTopNCount`（仅前N次数）+ `stockTopNCountMode`（谓词/排行切换）
2. 命名对齐前端：
   - "前N行业（次数）" → **"仅行业次数前N"**
   - "前N行业（涨幅）" → **"仅行业涨幅前N"**
   - "前N区间次数" → **"仅前区间次数"**（按用户要求）
   - 新增 → **"仅前N次数"**
3. getStockConfig() 增加 `topn_count` + `topn_count_mode`

> 注：后端 config/filters/pipeline **已支持** stock_topn_count（之前已实现），只是前端没暴露控件。

---

## 问题3：债券过滤配置缺失

### 现状（backtrace.html 债券侧现有控件）
| 控件 | ID |
|------|-----|
| 隐藏绿名单 | bondGreenFilter |
| 前N行业（涨幅） | bondTopNIndustryPct |
| 前N金额 | bondTopNAmount (+Mode) |
| 前N区间次数 | bondTopNWindow (+Mode) |

### 用户要求债券应有
1. 仅行业涨幅前N ✅ 已有（bondTopNIndustryPct）
2. 仅前N金额 ✅ 已有（bondTopNAmount）
3. 仅前N区间次数 ✅ 已有（bondTopNWindow）
4. **仅前N次数** ❌ 缺失
5. 隐藏绿名单 ✅ 已有（bondGreenFilter，但见问题1未生效）

### 修复方案（债券侧）
1. 新增控件 `bondTopNCount`（仅前N次数）+ `bondTopNCountMode`
2. 命名对齐："前N行业（涨幅）"→"仅行业涨幅前N"，"前N金额"→"仅前N金额"，"前N区间次数"→"仅前区间次数"
3. getBondConfig() 增加 `bond_topn_count` + `bond_topn_count_mode`

> 注：后端已支持 bond_topn_count，仅前端缺控件。

---

## 修复清单汇总

### 后端（backtrace.py）
- [ ] enrich 后补充绿名单标记（is_green / is_green_bond），支持当天Redis/历史MySQL双源

### 前端（backtrace.html）
- [ ] 股票侧新增"仅前N次数"控件 + 模式切换
- [ ] 债券侧新增"仅前N次数"控件 + 模式切换
- [ ] 全部控件命名对齐前端 monitor（仅行业次数前N/仅行业涨幅前N/仅前区间次数/仅前N次数/仅前N金额）

### 前端（backtrace.js）
- [ ] getStockConfig 增加 topn_count + topn_count_mode
- [ ] getBondConfig 增加 bond_topn_count + bond_topn_count_mode

### 验证
- [ ] 用真实数据验证：勾选债券隐藏绿名单后，交集中不再出现绿名单债券对应的股债对（如300996若123261是绿名单）
- [ ] 验证"仅前N次数"过滤生效

---

## 需你确认的点

1. **问题1语义**：债券隐藏绿名单 = 转债在绿名单就剔除该股债对（语义A），对吗？
2. **命名**："仅前区间次数"是否要带N（"仅前N区间次数"）？你说的是"仅前区间次数"，我按你说的去掉N，但前端 monitor 里是"仅前N区间次数"，需统一确认。

---

**等待审核**
