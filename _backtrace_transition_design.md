# 股债交集回溯系统 - 过渡期方案设计

## 1. 过渡期核心原则

**原则**: 零中断迁移，新旧系统并行运行，验证通过后切换

**策略**: 蓝绿部署模式
- 旧系统（前端过滤）保持运行
- 新系统（后端统一Pipeline）并行开发
- 验证通过后，前端切换调用后端API

---

## 2. 过渡期架构

### 2.1 阶段划分

```
┌─────────────────────────────────────────────────────────────────┐
│                        阶段1：开发期（4天）                       │
│  ┌─────────────────────┐    ┌─────────────────────┐             │
│  │   旧系统（运行中）   │    │   新系统（开发中）   │             │
│  │  前端JS过滤逻辑      │    │  后端UnifiedPipeline │             │
│  │  用户正常使用       │    │  独立开发测试        │             │
│  └─────────────────────┘    └─────────────────────┘             │
│                              │                                  │
│                              ▼                                  │
│                       内部测试验证                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        阶段2：并行期（3天）                        │
│  ┌─────────────────────┐    ┌─────────────────────┐             │
│  │   旧系统（主）       │    │   新系统（备）       │             │
│  │  前端JS过滤（默认）   │    │  后端过滤（可选）    │             │
│  │  用户可切换对比      │    │  验证一致性          │             │
│  └─────────────────────┘    └─────────────────────┘             │
│                              │                                  │
│                              ▼                                  │
│                       一致性验证通过                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        阶段3：切换期（1天）                        │
│  ┌─────────────────────┐    ┌─────────────────────┐             │
│  │   旧系统（备用）     │    │   新系统（主）       │             │
│  │  保留回滚能力        │    │  前端调用后端API     │             │
│  │  紧急情况可回退       │    │  成为默认模式        │             │
│  └─────────────────────┘    └─────────────────────┘             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        阶段4：清理期（可选）                       │
│  ┌─────────────────────┐                                       │
│  │   旧系统（下线）     │                                       │
│  │  前端JS过滤代码删除  │                                       │
│  │  仅保留后端统一Pipeline│                                     │
│  └─────────────────────┘                                       │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 技术实现

#### 阶段1：开发期（4天）

**目标**: 后端UnifiedPipeline开发完成，内部测试通过

**实施内容**:
1. 创建 `src/gs2026/common/pipeline/` 目录
2. 实现所有过滤器（不改动前端）
3. 实现 `/api/filter/stock` 和 `/api/filter/bond` API
4. 内部测试：对比前后端过滤结果一致性

**代码结构**:
```
src/gs2026/common/pipeline/          # 【新增】
├── __init__.py
├── pipeline.py                      # UnifiedPipeline
├── config.py                        # FilterConfig
└── filters/
    ├── __init__.py
    ├── base.py
    ├── predicate.py
    └── ranking.py

src/gs2026/dashboard2/routes/
├── monitor.py                       # 【不变】前端过滤保持
└── filter_api.py                    # 【新增】后端过滤API
```

**关键**：前端代码完全不改动，用户无感知

#### 阶段2：并行期（3天）

**目标**: 新旧系统并行，用户可切换，验证一致性

**实施内容**:
1. 前端添加"使用后端过滤"开关（默认关闭）
2. 开关开启时，前端调用后端API获取过滤结果
3. 添加对比模式：同时显示新旧系统结果
4. 监控一致性差异

**前端改动**:
```javascript
// monitor.html 添加切换开关
<div class="filter-mode-toggle">
    <label>
        <input type="checkbox" id="useBackendFilter" onchange="toggleFilterMode()">
        使用后端过滤（测试模式）
    </label>
</div>

// 过滤函数改造
async function runPipeline(pipeline, data) {
    if (document.getElementById('useBackendFilter').checked) {
        // 调用后端API
        return await callBackendFilter(pipeline, data);
    } else {
        // 使用原有前端逻辑
        return originalRunPipeline(pipeline, data);
    }
}
```

**一致性验证**:
```python
# 验证脚本
for each tick in test_date:
    stock_data = fetch_stock(tick)
    bond_data = fetch_bond(tick)
    
    frontend_result = frontend_filter(stock_data, config)
    backend_result = backend_filter(stock_data, config)
    
    assert frontend_result == backend_result, f"不一致 at {tick}"
```

#### 阶段3：切换期（1天）

**目标**: 新系统成为默认，旧系统保留回滚能力

**实施内容**:
1. 将"使用后端过滤"开关默认开启
2. 前端JS过滤代码保留但默认不执行
3. 添加回滚机制：紧急情况下可快速回退

**回滚命令**:
```bash
# 快速回滚到前端过滤
git revert HEAD  # 回退切换提交
# 或修改配置
echo "USE_BACKEND_FILTER=false" > .env
```

#### 阶段4：清理期（可选，1天）

**目标**: 删除旧代码，仅保留后端统一Pipeline

**实施内容**:
1. 删除前端JS过滤逻辑
2. 删除"使用后端过滤"开关
3. 前端直接调用后端API

---

## 3. 风险控制

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 后端过滤性能差 | 中 | 高 | 阶段2并行测试，监控响应时间，>500ms则优化 |
| 前后端结果不一致 | 中 | 高 | 阶段2对比模式，发现差异立即修复 |
| 用户不适应新方式 | 低 | 中 | 保留旧系统作为备选，逐步引导 |
| 网络延迟影响体验 | 中 | 中 | 添加本地缓存，批量请求优化 |

---

## 4. 过渡期时间表

| 阶段 | 工期 | 关键产出 | 验收标准 |
|------|------|----------|----------|
| 阶段1：开发期 | 4天 | UnifiedPipeline实现 | 内部测试通过 |
| 阶段2：并行期 | 3天 | 对比验证报告 | 一致性100% |
| 阶段3：切换期 | 1天 | 切换完成 | 新系统成为默认 |
| 阶段4：清理期 | 1天（可选） | 代码清理 | 旧代码删除 |
| **总计** | **8-9天** | | |

---

## 5. 回滚机制

### 5.1 快速回滚（1分钟内）

```bash
# 方案1：配置切换
echo "USE_BACKEND_FILTER=false" > .env
systemctl restart gs2026

# 方案2：Git回滚
git revert HEAD --no-edit
systemctl restart gs2026
```

### 5.2 完整回滚（5分钟内）

```bash
# 回退到阶段1之前
git reset --hard <stage1-start-commit>
systemctl restart gs2026
```

---

## 6. 验证检查点

| 检查点 | 阶段 | 检查内容 | 通过标准 |
|--------|------|----------|----------|
| 单元测试 | 阶段1 | 过滤器测试 | 100%通过 |
| 性能测试 | 阶段1 | API响应时间 | <500ms |
| 一致性测试 | 阶段2 | 前后端结果对比 | 差异率0% |
| 用户验收 | 阶段2 | 功能可用性 | 无阻塞问题 |
| 监控指标 | 阶段3 | 错误率、响应时间 | 错误率<0.1% |

---

**文档状态**: 待审核
