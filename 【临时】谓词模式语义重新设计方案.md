# 谓词模式重新设计

**分析日期**: 2026-08-03  
**需求澄清**: predicate vs ranking 的语义区分

---

## 语义澄清

### predicate 模式（谓词模式）

**设计意图**: 从**全部原始数据**中，按条件过滤后取前N条

**执行流程**:
1. 输入：全部原始数据
2. 过滤：保留金额>0的数据
3. 排序：按金额倒序
4. 截取：取前N条
5. 输出：前N条金额最大的数据

**特点**:
- 基于**全部数据**计算
- 不依赖前面过滤器的输出
- 结果集大小 = min(N, 符合条件的数量)

### ranking 模式（排行模式）

**设计意图**: 从**前面过滤器已经过滤后的候选池**中，取前N条

**执行流程**:
1. 输入：前面所有谓词过滤器处理后的候选池 S
2. 排序：按金额倒序
3. 截取：取前N条
4. 输出：候选池 S 中金额最大的前N条

**特点**:
- 基于**候选池 S** 计算
- 依赖前面过滤器的输出
- 结果集大小 = min(N, 候选池大小)

---

## 当前实现的问题

### 当前 `runPipeline` 实现

```javascript
function runPipeline(pipeline, data) {
    // Phase 1: 谓词型串行 → 候选池 S
    let S = data;
    for (const id in pipeline) {
        const f = pipeline[id];
        if (getFilterKind(pipeline, id) === 'predicate') {
            if (!f.isActive || f.isActive()) S = f.apply(S, 'predicate');
        }
    }
    
    // Phase 2: 排名型各自基于 S 算，取交集
    const rankingIds = Object.keys(pipeline).filter(
        id => getFilterKind(pipeline, id) === 'ranking'
    );
    
    let finalSets = [];
    for (const id of rankingIds) {
        const f = pipeline[id];
        if (!f.isActive || f.isActive()) {
            const r = f.apply(S, 'ranking');
            finalSets.push(new Set(r.map(x => x.code)));
        }
    }
    
    // ... 取交集逻辑
}
```

### 问题

当前实现中，**谓词模式也参与候选池 S 的构建**，这意味着：
1. 谓词过滤器的输入是前面谓词过滤器的输出 S
2. 不是直接从全部原始数据计算

这与设计意图不符。

---

## 修复方案

### 方案：修改 `runPipeline`，让 predicate 模式直接从原始数据计算

```javascript
function runPipeline(pipeline, data) {
    // 【修改】分离 predicate 模式为两类：
    // 1. 纯谓词（如 bond_mode, green_list）：参与候选池 S 构建
    // 2. 带N的谓词（如 topn_amount, topn_window）：从原始数据独立计算
    
    // Phase 0: 纯谓词型串行 → 候选池 S
    let S = data;
    for (const id in pipeline) {
        const f = pipeline[id];
        const kind = getFilterKind(pipeline, id);
        // 【修改】纯谓词（fixed=true 或没有 selectId）才参与候选池
        if (kind === 'predicate' && f.fixed) {
            if (!f.isActive || f.isActive()) S = f.apply(S, 'predicate');
        }
    }
    
    // Phase 1: 带N的谓词（从原始数据独立计算）
    let predicateSets = [];
    for (const id in pipeline) {
        const f = pipeline[id];
        const kind = getFilterKind(pipeline, id);
        // 【修改】带N的谓词（fixed=false）从原始数据 data 计算
        if (kind === 'predicate' && !f.fixed) {
            if (!f.isActive || f.isActive()) {
                const r = f.apply(data, 'predicate');  // 注意：使用 data 而非 S
                predicateSets.push(new Set(r.map(x => x.code)));
            }
        }
    }
    
    // Phase 2: 排名型基于候选池 S 计算
    let rankingSets = [];
    for (const id in pipeline) {
        const f = pipeline[id];
        if (getFilterKind(pipeline, id) === 'ranking') {
            if (!f.isActive || f.isActive()) {
                const r = f.apply(S, 'ranking');
                rankingSets.push(new Set(r.map(x => x.code)));
            }
        }
    }
    
    // Phase 3: 所有集合取交集
    const allSets = [...predicateSets, ...rankingSets];
    if (allSets.length === 0) return S;
    
    let finalSet = allSets[0];
    for (let i = 1; i < allSets.length; i++) {
        finalSet = new Set([...finalSet].filter(x => allSets[i].has(x)));
    }
    
    return data.filter(it => finalSet.has(it.code));
}
```

### 同时修改 `applyToggleableFilter`

```javascript
function applyToggleableFilter(data, mode, selectId, field) {
    const el = document.getElementById(selectId);
    if (!el) return data;
    const n = parseInt(el.value) || 0;
    
    // 【修改】谓词模式：从输入数据（全部原始数据）中过滤后取前N
    if (mode === 'predicate') {
        // 1. 过滤出金额>0的数据
        const filtered = data.filter(it => (parseFloat(it[field]) || 0) > 0);
        
        // 2. 按金额倒序
        const sorted = filtered.sort((a, b) => (parseFloat(b[field]) || 0) - (parseFloat(a[field]) || 0));
        
        // 3. 取前N条（如果 n > 0）
        if (n > 0) {
            return sorted.slice(0, n);
        }
        
        return sorted;
    }
    
    // 【不变】排行模式：从候选池 S 中取前N
    if (n <= 0) return data;
    const sorted = data.slice()
        .filter(it => (parseFloat(it[field]) || 0) > 0)
        .sort((a, b) => (parseFloat(b[field]) || 0) - (parseFloat(a[field]) || 0));
    const codes = {};
    for (let i = 0; i < Math.min(n, sorted.length); i++) codes[sorted[i].code] = true;
    return data.filter(it => codes[it.code]);
}
```

---

## 对比

### 场景：债券数据100条，金额>0的有80条，设置"仅前N金额=20"

| 模式 | 数据来源 | 计算方式 | 结果 |
|------|----------|----------|------|
| **predicate** | 全部100条 | 金额>0的80条 → 取前20 | 20条（金额最大的20条） |
| **ranking** | 候选池S | 假设S有50条 → 取前20 | 20条（候选池中金额最大的20条） |

### 关键区别

- **predicate**: 从全部数据计算，不受其他过滤器影响
- **ranking**: 从候选池计算，受其他谓词过滤器影响

---

## 验证方法

1. 设置"仅前N金额"为谓词模式，数量=20
2. 观察结果是否为金额最大的20条（从全部数据计算）
3. 同时启用其他过滤器（如"隐藏绿名单"）
4. 观察谓词模式的结果是否**不受**"隐藏绿名单"影响
5. 切换到排行模式，观察结果是否**受**"隐藏绿名单"影响

---

**等待用户审核修复方案**
