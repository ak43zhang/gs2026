# 股债交集回溯系统 - 用户期望确认

## 用户期望

> "股债交集回溯系统是不是可以自定义条件然后点击执行后得到自定义条件的结果"

**期望流程**:
```
用户选择过滤条件 → 点击"开始回溯" → 系统执行 → 显示结果
```

## 当前方案设计

### 方案确认: ✅ **完全符合用户期望**

当前方案设计正是：
1. **自定义条件**: 用户在控制面板选择过滤条件（行业、前N、排除绿名单等）
2. **点击执行**: 点击"开始回溯"按钮
3. **得到结果**: 系统遍历时间轴，执行过滤，计算交集，保存并展示结果

### 详细对应

| 用户期望 | 当前方案 | 对应章节 |
|----------|----------|----------|
| 自定义条件 | 控制面板：股票过滤配置 + 债券过滤配置 | 15.3 控制面板设计 |
| 点击执行 | "开始回溯"按钮 → 调用 `/api/backtrace/run` | 15.6 前端交互流程 |
| 得到结果 | 结果展示表格 + 分页 + 导出 | 15.5 结果展示表格 |

### 交互流程对比

**用户期望**:
```
[选择日期] → [选择股票过滤条件] → [选择债券过滤条件] → [点击执行] → [查看结果]
```

**当前方案**:
```
[日期选择] → [股票过滤配置行] → [债券过滤配置行] → [开始回溯按钮] → [结果表格展示]
    ↓
调用 POST /api/backtrace/run
    ↓
遍历时间轴 → 过滤股票 → 过滤债券 → 计算交集 → 保存MySQL
    ↓
展示结果表格（股票信息 + 债券信息）
```

### 过滤条件自定义能力

**股票过滤条件**:
- 行业筛选（全部/电子/银行等）
- 前N行业（次数）
- 前N行业（涨幅）
- 前N区间次数
- 前N累计次数
- 仅显示有债券的（复选框）

**债券过滤条件**:
- 行业筛选
- 前N行业（次数）
- 前N行业（涨幅）
- 前N金额
- 前N区间次数
- 前N累计次数
- 排除绿名单（复选框）

### 结果展示

**表格列**:
| 序号 | 股票代码 | 股票名称 | 股票涨跌幅 | 股票累计次数 | 债券代码 | 债券名称 | 债券涨跌幅 | 债券金额 | 操作 |

**功能**:
- 分页浏览
- 每页条数选择（20/50/100）
- 导出Excel
- 查看详情

## 结论

**当前方案完全符合用户期望**

无需修改，当前设计已经是：
- ✅ 自定义条件（丰富的过滤配置）
- ✅ 点击执行（开始回溯按钮）
- ✅ 得到结果（表格展示 + 导出）

---

**确认时间**: 2026-08-03 22:10  
**确认人**: AI Assistant
